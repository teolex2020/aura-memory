---
phase: "06.3"
fixed_at: "2026-05-29T23:30:00Z"
review_path: ".planning/phases/06.3-engine-algorithm-parity/06.3-REVIEW.md"
iteration: 3
findings_in_scope: 9
fixed: 9
skipped: 0
status: all_fixed
---

# Phase 06.3: Code Review Fix Report (Re-review)

**Fixed at:** 2026-05-29T23:30:00Z
**Source review:** .planning/phases/06.3-engine-algorithm-parity/06.3-REVIEW.md
**Iteration:** 3 (re-review fix pass after iteration 2)

**Summary:**
- Findings in scope: 9 (5 warnings + 4 info)
- Fixed: 9
- Skipped: 0
- Status: all_fixed

## Fixed Issues

### WR-01: BrainAuraRecord structurally incompatible with AuraRecord in runMaintenance

**Files modified:** `packages/core/src/Aura.ts`
**Commit:** `7b341a4`
**Applied fix:** Replaced the unsafe `r as unknown as AuraRecord` cast in `runMaintenance()` with `yield* loadCognitiveRecords(dir)`, which returns properly normalized `AuraRecord` via `normalizeCognitiveRecord()`, consistent with `store()`, `update()`, and `connect()` methods. Removed the intermediate `recordsList` variable.

### WR-02: generateRecommendation uses raw string literals instead of PolicyActionKind enum

**Files modified:** `packages/policy/src/PolicyEngine.ts`
**Commit:** `9169ac0`
**Applied fix:** Replaced raw string literals (`"avoid"`, `"verify_first"`, `"prefer"`, `"recommend"`, `"warn"`) with the corresponding `PolicyActionKind` enum values (`PolicyActionKind.Avoid`, `PolicyActionKind.VerifyFirst`, `PolicyActionKind.Prefer`, `PolicyActionKind.Recommend`, `PolicyActionKind.Warn`) in the `generateRecommendation` switch statement.

### WR-03: Dead timing code in runDiscoveryPhases -- feedbackMs never measured

**Files modified:** `packages/core/src/MaintenanceService.ts`
**Commit:** `e95c5d6`
**Applied fix:** Removed the dead `t = Date.now()` assignment in the feedback phase of `runDiscoveryPhases`. Replaced with a clear TODO comment documenting where `feedbackMs` timing instrumentation should be added when the real `apply_layer_feedback()` is integrated and `PhaseTimings` gains a `feedbackMs` field.

### WR-04: Policy Surface.ts uses deprecated local interfaces incompatible with contract

**Files modified:** `packages/policy/src/Surface.ts`
**Commit:** `f0071ac`
**Applied fix:** Added `policyEngineFromState(state: PolicyEngineState): PolicyEngine` adapter function that bridges the contract's `PolicyEngineState` (from `engine.stats()`) to the local flat `PolicyEngine` container shape used by surface functions. Updated JSDoc on the deprecated local `PolicyHint` and `PolicyEngine` interfaces to reference the adapter and document field mappings (camelCase to snake_case). Imported `PolicyEngineState` and `PolicyHint as ContractPolicyHint` from `@aura/contract`.

### WR-05: computeCorpusFingerprint hash drift -- might differ between first and subsequent cycles

**Files modified:** `packages/causal/src/CausalEngine.ts`
**Commit:** `abd3bad`
**Applied fix:** Added eager hasher initialization via `if (!_hasher) { _hasher = yield* Effect.promise(() => xxhash()) }` in the `discover()` method before `computeCorpusFingerprint` is called. This ensures the same hash algorithm (xxhash-wasm) is used across all maintenance cycles, preventing fingerprint mismatch that would defeat the skip-unchanged optimization. **Flag for human verification:** The `Effect.promise` error type compatibility with the `never` error channel in `discover()`'s return type should be verified against Effect 4.0.0-beta.68 semantics.

### IN-01: Surface.test.ts mock uses deprecated ConceptEngineImpl with as-cast

**Files modified:** `packages/concept/src/Surface.test.ts`
**Commit:** `34e0c1b`
**Applied fix:** Replaced the deprecated `ConceptEngineImpl` type import and return type with `ConceptEngine.Interface` in the `mockEngine` test helper. Removed the unnecessary `as unknown as ConceptEngineImpl` cast since the returned object shape (`{ stats: () => Effect.succeed(state) }`) satisfies `ConceptEngine.Interface` directly.

### IN-02: buildHints receives beliefEngine but aggregateBeliefConfidence bypasses it

**Files modified:** `packages/policy/src/PolicyEngine.ts`
**Commit:** `1f81139`
**Applied fix:** Added detailed JSDoc parameter documentation to `aggregateBeliefConfidence` explaining that `beliefEngine` is only consumed in Phases 2-3 (the fallback path when `beliefs` is NOT provided). In the primary integration path through `discover()`, cached `beliefs` are always passed so the parameter is deliberately unused.

### IN-03: Test helper mock factories use `as any` for typed enums

**Files modified:** `packages/policy/src/PolicyEngine.test.ts`, `packages/causal/src/CausalEngine.test.ts`
**Commit:** `9b85eb6`
**Applied fix:**
- PolicyEngine.test.ts: Added imports for `ConceptSeedMode`, `ConceptSimilarityMode`, `ConceptPartitionMode`, `ConceptUnionMode` from `@aura/contract`. Replaced `"Standard" as any` and `"SdrTanimoto" as any` casts with proper enum value references.
- CausalEngine.test.ts: Added `Level` import from `@aura/contract`. Replaced `"Domain" as any` cast with `Level.Domain`.

### IN-04: dead code in EpistemicRuntime.test.ts (sampleBelief's hypothesis_ids set to empty)

**Files modified:** `packages/epistemic-runtime/src/EpistemicRuntime.test.ts`
**Commit:** `0c821a0`
**Applied fix:** Added comment explaining that `hypothesis_ids: []` is a deliberate default because `getContradictionClusters` reads from `state.hypotheses` (via `beliefEngine.stats()`) rather than from `belief.hypothesis_ids`. The contradiction cluster computation builds record-to-belief mappings from `hypothesis.prototype_record_ids`, so the field is dead data in test contexts.

---

_Fixed: 2026-05-29T23:30:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 3_
