---
phase: 06.2
plan: 01
subsystem: contract-types
tags: [maintenance, epistemic-inspection, type-projection, rust-alignment]

# Dependency graph
requires:
  - phase: 06.2-PROJECTION
    provides: "Rust → TS type mapping specification for all MaintenanceService and EpistemicRuntime types"
provides:
  - "Maintenance.ts with 25+ type/interface/enum exports (ArchivalRule through MaintenanceReport)"
  - "EpistemicInspection.ts with 15+ type exports (BeliefInstabilitySummary through SurfacedPolicyHint)"
  - "EpistemicRuntime.Interface refactored to namespace.Interface pattern with 18 inspection methods"
  - "PolicyActionKind enum, extended PolicyHint with 5 new fields, PolicyEngineState.key_index"
  - "index.ts re-exports for Maintenance and EpistemicInspection modules"
affects: [06.2-02, 06.2-03, 06.2-04, 06.2-05, 06.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "namespace.Interface pattern for EpistemicRuntime (matching BeliefEngine precedent)"
    - "All contract types use readonly fields per @aura/contract convention"
    - "camelCase naming for runtime report types (not disk-format snake_case)"
    - "ConceptSurfaceCounters using Effect Ref.Ref<number> for telemetry modeling"

key-files:
  created:
    - packages/contract/src/Maintenance.ts
    - packages/contract/src/EpistemicInspection.ts
  modified:
    - packages/contract/src/EpistemicRuntime.ts
    - packages/contract/src/policy/PolicyTypes.ts
    - packages/contract/src/index.ts

key-decisions:
  - "Used nested LayerChurn/LayerStability types instead of Rust's flat field approach (more idiomatic TS)"
  - "MaintenanceReport placed in Maintenance.ts with cross-file type imports from EpistemicInspection.ts (ConceptSurfaceTelemetry, ReflectionSummary)"
  - "getContradictionClusters takes records as first parameter per RESEARCH.md Open Question #3"
  - "Import paths corrected from plan defaults — Belief from ./belief/BeliefTypes (not ./Belief)"

patterns-established:
  - "Cross-file type-only imports: Maintenance.ts ← EpistemicInspection.ts (circular deps safe via import type)"
  - "Pure type files: no runtime code, no side effects, no default exports"
  - "Enum string values match Rust Display impls exactly (e.g., ConceptSurfaceMode.Inspect = \"Inspect\")"

requirements-completed: [REQ-011, REQ-012]

# Metrics
duration: ~25min
completed: 2026-05-28
---

# Phase 06.2 Plan 01: MaintenanceService and EpistemicRuntime Contract Types

**30+ TypeScript type definitions aligned 1:1 with Rust upstream — MaintenanceConfig, all phase report types, EpistemicRuntime.Interface with 18 inspection methods, PolicyActionKind enum, and surface types**

## Performance

- **Duration:** ~25 min
- **Tasks:** 3
- **Files created:** 2
- **Files modified:** 3

## Accomplishments

- Created Maintenance.ts with all maintenance-related types: ArchivalRule, CompletedArchivalRule, MaintenanceConfig (with exact 10-rule default from Rust), PhaseTimings, LayerStability, 8 phase report types, FeedbackAuditEntry/Report, ConceptSurfaceMode enum, MaintenanceHotspots (25 fields), MaintenanceTrendSnapshot, MaintenanceTrendSummary, and MaintenanceReport (22 fields)
- Created EpistemicInspection.ts with all epistemic inspection types: BeliefInstabilitySummary, ContradictionCluster, PolicyLifecycleSummary/PolicyPressureArea, ConceptSurfaceTelemetry, ReflectionFinding/Summary/Digest, SurfacedConcept, and SurfacedPolicyHint
- Refactored EpistemicRuntime.ts from flat `EpistemicRuntimeImpl` type to `namespace EpistemicRuntime { Interface }` pattern matching BeliefEngine, with 18 inspection methods and no maintain()
- Extended PolicyHint with 5 new fields (actionKind, policyStrength, riskScore, namespace, domain) and PolicyEngineState with key_index
- Added PolicyActionKind enum (Prefer/Recommend/VerifyFirst/Avoid/Warn) to PolicyTypes.ts
- Updated index.ts to re-export both new modules

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Maintenance.ts** - `49a3bfd` (feat)
2. **Task 2: Create EpistemicInspection.ts** - `5504b7f` (feat)
3. **Task 3: Update EpistemicRuntime.ts, PolicyTypes.ts, index.ts** - `9195359` (feat)

## Files Created/Modified

- `packages/contract/src/Maintenance.ts` — 25+ type/interface/enum exports: ArchivalRule, MaintenanceConfig, defaultMaintenanceConfig, PhaseTimings, LayerChurn, LayerStability, DecayReport, ReflectReport, EpistemicPhaseReport, BeliefPhaseReport, ConceptPhaseReport, CausalPhaseReport, PolicyPhaseReport, FeedbackAuditEntry, FeedbackAuditReport, ConsolidationReport, ConceptSurfaceMode, MaintenanceHotspots, MaintenanceTrendSnapshot, MaintenanceTrendSummary, InitialMaintenancePhaseResult, DiscoveryPhaseResult, PostDiscoveryPhaseResult, ConceptSurfaceCounters, MaintenanceReport
- `packages/contract/src/EpistemicInspection.ts` — 15+ type exports: BeliefVolatilityBands, BeliefInstabilitySummary, ContradictionCluster, PolicyActionSummary, PolicyDomainSummary, PolicyPressureArea, PolicyLifecycleSummary, ConceptSurfaceTelemetry, ReflectionFinding, ReflectionJobReport, ReflectionSummary, ReflectionKindSummary, ReflectionDigest, SurfacedConcept, SurfacedPolicyHint
- `packages/contract/src/EpistemicRuntime.ts` — Complete rewrite: removed maintain(), old snake_case getters, and stub methods; added namespace.Interface with 18 inspection methods across all 4 layers
- `packages/contract/src/policy/PolicyTypes.ts` — Added PolicyActionKind enum; extended PolicyHint with 5 fields; added key_index to PolicyEngineState
- `packages/contract/src/index.ts` — Added export lines for ./Maintenance and ./EpistemicInspection

## Decisions Made

- Used nested `LayerChurn`/`LayerStability` types instead of Rust's flat 16-field approach for better TypeScript ergonomics
- `MaintenanceReport` placed in `Maintenance.ts` using cross-file `import type` from `EpistemicInspection.ts` for `ConceptSurfaceTelemetry` and `ReflectionSummary`
- `getContradictionClusters` takes `records: ReadonlyMap<string, AuraRecord>` as first parameter per RESEARCH.md Open Question #3 resolution
- Import paths corrected from plan defaults: `Belief` imported from `./belief/BeliefTypes` (not `./Belief`), similarly for `ConceptCandidate`, `CausalPattern`, and `PolicyHint`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed import paths in EpistemicRuntime.ts**

- **Found during:** Task 3 (EpistemicRuntime.ts rewrite)
- **Issue:** Plan specified imports like `import type { Belief, BeliefEngine } from "./Belief"` but `Belief` type is exported from `./belief/BeliefTypes`, not `./Belief`. Similar issues for `ConceptCandidate`, `CausalPattern`, and `PolicyHint`.
- **Fix:** Used correct import paths: `Belief` from `"./belief/BeliefTypes"`, `ConceptCandidate` from `"./concept/ConceptTypes"`, `CausalPattern` from `"./causal/CausalTypes"`, `PolicyHint` from `"./policy/PolicyTypes"`.
- **Files modified:** `packages/contract/src/EpistemicRuntime.ts`
- **Committed in:** `9195359` (Task 3 commit)

**2. [Rule 1 - Bug] Removed unused imports that would cause TypeScript errors**

- **Found during:** Task 3 code review
- **Issue:** EpistemicInspection.ts had unused `import type { ConceptSurfaceMode } from "./Maintenance"`. EpistemicRuntime.ts had unused `import type { ConceptSurfaceMode } from "./Maintenance"`. Neither file used `ConceptSurfaceMode` in its type definitions.
- **Fix:** Removed both unused imports.
- **Files modified:** `packages/contract/src/EpistemicInspection.ts`, `packages/contract/src/EpistemicRuntime.ts`
- **Committed in:** `9195359` (Task 3 commit — EpistemicInspection.ts amend staged)

**3. [Rule 3 - Blocking] Corrected file write path from main repo to worktree**

- **Found during:** Task 1 initial commit
- **Issue:** Initial `Maintenance.ts` was written to main repo path (`D:/workspace/@yuyi919/external/AuraSDK/typescript/...`) instead of worktree path (`D:/workspace/@yuyi919/external/AuraSDK/.claude/worktrees/agent-ae33e9aa804976670/typescript/...`).
- **Fix:** Removed file from main repo, re-wrote to correct worktree path using `git rev-parse --show-toplevel` for path derivation.
- **Files modified:** `packages/contract/src/Maintenance.ts`
- **Committed in:** `49a3bfd` (Task 1 commit)

---

**Total deviations:** 3 auto-fixed (2 blocking, 1 bug)
**Impact on plan:** All auto-fixes necessary for correctness. No scope creep. Import path corrections are within the plan's intent — same types, correct sources.

## Issues Encountered

- **Typecheck environment:** `bun run typecheck` fails with pre-existing `TS2688: Cannot find type definition file for 'node'` and `'vitest/globals'` errors — these are missing `@types/node` and `vitest` in the worktree's `node_modules`, unrelated to code changes. Code verified correct via manual review of all type definitions against PROJECTION.md, 18-method count check, and grep-based export verification.

## Next Phase Readiness

- All contract types for MaintenanceService (Plan 02) and EpistemicRuntime refactor (Plan 03/04) are defined and exported
- EpistemicRuntime.Interface uses namespace.Interface pattern matching BeliefEngine — implementation can `implements EpistemicRuntime.Interface`
- `packages/epistemic-runtime` will need its `EpistemicRuntimeImpl` updated to implement the new Interface (expected compilation error until Plan 04)
- PolicyEngine implementation will need to populate new PolicyHint fields (actionKind, policyStrength, riskScore, namespace, domain)

---
*Phase: 06.2-epistemicruntime-maintain-maintenanceservice-rust*
*Completed: 2026-05-28*
