---
phase: 06.3-engine-algorithm-parity
plan: 03
type: tdd
wave: 2
depends_on: [06.3-02]
files_modified:
  - packages/belief/src/BeliefEngine.ts
  - packages/belief/src/BeliefEngine.test.ts
autonomous: true
requirements: [REQ-011]
user_setup: []

must_haves:
  truths:
    - "SDR subcluster has 4 guard strategies: plain, tag_guarded, bridge_guarded, tag_sdr_guarded"
    - "Each strategy dispatches based on coarse_key_mode: standard→plain, DualKey→tag_guarded, BridgeKey→plain with version-2 threshold, SdrTagPool→tag_sdr_guarded"
    - "Subcluster dispatch table verified against Rust belief.rs source (search for match coarse_key_mode in update_with_sdr)"
    - "Standard coarse key truncates tags to top 3 (not all tags)"
    - "TagFamily coarse key uses alphabetically first tag (not prefix extraction)"
    - "DualKey coarse key produces namespace:semantic_type (no tags)"
    - "NeighborhoodPool coarse key produces namespace:semantic_type (no tags)"
  artifacts:
    - path: packages/belief/src/BeliefEngine.ts
      provides: "4 SDR subcluster functions + updated claim_key_with_mode"
      contains: "sdr_subcluster_tag_guarded, sdr_subcluster_bridge_guarded, sdr_subcluster_tag_sdr_guarded"
    - path: packages/belief/src/BeliefEngine.test.ts
      provides: "Tests for all 4 subcluster strategies + coarse key mode fixes"
  key_links:
    - from: packages/belief/src/BeliefEngine.ts
      to: @../src/belief.rs
      via: "Rust source for subcluster functions — ALL thresholds extracted directly from belief.rs, not secondary documents"
---

<objective>
Implement 4 SDR subcluster guard strategies (plain/tag_guarded/bridge_guarded/tag_sdr_guarded) and fix coarse key mode alignment (Standard truncation, TagFamily alphabetically-first, DualKey/NeighborhoodPool semantic_only).

Purpose: Close AUDIT-DIFF 1.2 and 1.3 — the 5-vs-1 SDR subcluster strategy gap and 3 coarse key mode differences.
Output: BeliefEngine.ts with Rust-aligned subcluster dispatch and coarse key modes, passing test suite.
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06.3-engine-algorithm-parity/06.3-CONTEXT.md
@.planning/phases/06.3-engine-algorithm-parity/06.3-RESEARCH.md
@AUDIT-DIFF.md

# Rust reference for subcluster functions — ALL thresholds/dispatch rules extracted directly from Rust source, not from secondary summaries
@../src/belief.rs

# Current belief engine (after P0 changes from plan 02)
@packages/belief/src/BeliefEngine.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement 4 SDR subcluster guard functions + coarse key mode alignment</name>
  <files>
    packages/belief/src/BeliefEngine.ts
    packages/belief/src/BeliefEngine.test.ts
  </files>
  <read_first>
    packages/belief/src/BeliefEngine.ts
    @../src/belief.rs (lines 678-975 for subcluster functions, lines 455-525 for claim_key_with_mode)
  </read_first>
  <behavior>
    Test 1: sdr_subcluster (plain) uses threshold 0.15 only, Union-Find
    Test 2: sdr_subcluster_tag_guarded requires shared_tags >= 1 before Tanimoto check
    Test 3: sdr_subcluster_bridge_guarded requires shared bridge tag before Tanimoto check
    Test 4: sdr_subcluster_tag_sdr_guarded requires tag fingerprint + content SDR dual guard
    Test 5: Standard coarse key mode truncates sorted tags to top 3
    Test 6: TagFamily mode uses alphabetically first tag (not prefix extraction)
    Test 7: DualKey mode produces namespace:semantic_type (no tags in key)
    Test 8: NeighborhoodPool mode produces namespace:semantic_type (no tags in key)
    Test 9: BridgeKey mode produces namespace:normalized_tag:bridge:semantic_type
    Test 10: SdrTagPool mode produces sorted_tags:semantic_type
  </behavior>
  <action>
    Per D-03, D-08: Implement 4 subcluster functions and fix coarse key modes.

    CRITICAL — ALL thresholds and dispatch rules MUST be extracted directly from `@../src/belief.rs` using grep/read. The dispatch table below is a guide; verify each value against the Rust match statement in `update_with_sdr` or the relevant subcluster function's constant definitions.

    **SDR Subcluster Functions (4 variants) — read Rust source for exact threshold values:**

    1. sdr_subcluster(records, sdrLookup, threshold? = SDR_TANIMOTO_THRESHOLD)
       - Existing unionFindClusters logic, but threshold parameterized from caller
       - Union-Find with Tanimoto >= threshold

    2. sdr_subcluster_tag_guarded(records, sdrLookup, threshold):
       - Union-Find baseline
       - Tag barrier: require shared_tags >= 1 between i and j before Tanimoto check
       - Exception: if either side has no tags (tags.length === 0), skip tag barrier
       - Rust source belief.rs:741-803
       - Verify TAG_GUARD threshold from Rust (search for `TAG_GUARD_THRESHOLD` or the specific value used in tag_guarded dispatch)

    3. sdr_subcluster_bridge_guarded(records, sdrLookup, threshold):
       - Union-Find baseline
       - Bridge guard: require normalized bridge tag overlap
       - Normalize each record's first tag via normalizeBridgeTag (to be implemented in Plan 04)
       - If normalized tags share a common bridge → allow Tanimoto comparison
       - Rust source belief.rs:810-875

    4. sdr_subcluster_tag_sdr_guarded(records, sdrLookup, threshold):
       - Dual guard: tag fingerprint + content SDR
       - First pass: group by identical tag sets (Tag SDR)
       - Within tag group: apply SDR Tanimoto with threshold
       - If tag sets differ but fingerprint similarity >= TAG_SDR_FINGERPRINT_THRESHOLD: allow cross-group
       - Rust source belief.rs:882-975

    **update_with_sdr integration — dispatch table verified against Rust `match coarse_key_mode`:**

    For each coarse_key_mode, read the Rust `match` block in `update_with_sdr` (or equivalent dispatch function) to determine:
    - Which subcluster strategy to use
    - What threshold to pass

    Approximate mapping (verify against Rust source before implementing):
    - Standard / TopOneTag / SemanticOnly / SemanticTypeOnly → sdr_subcluster (plain)
    - TagFamily / TagFamilyAdaptive → sdr_subcluster (plain) 
    - DualKey → sdr_subcluster_tag_guarded
    - NeighborhoodPool → sdr_subcluster_tag_guarded
    - BridgeKey → sdr_subcluster_bridge_guarded
    - SdrTagPool → sdr_subcluster_tag_sdr_guarded
    - TagFamilyBackoff / TagFamilyDenseBackoff → (backoff-specific dispatch, not a standard subcluster entry)

    **Coarse Key Mode Changes** (claim_key_with_mode):

    Standard mode:
    - Change `activeTags.join(",")` to `activeTags.slice(0, 3).join(",")` — truncate to top 3
    - Verify Rust truncation behavior from belief.rs (search for `truncate(3)` or `take(3)` in claim_key_with_mode)

    TagFamily / TagFamilyAdaptive / TagFamilyBackoff / TagFamilyPairBackoff / TagFamilyDenseBackoff:
    - Change from prefix extraction to alphabetically first tag
    - Remove the "/" and "_" prefix logic
    - Use `activeTags.sort()[0]` or equivalent — take first sorted tag as family
    - Verify Rust alphabetically-first behavior from belief.rs (search for `sort` and `first` in TagFamily mode)

    DualKey:
    - Change from `${ns}:${activeTags.join(",")}:${st}` to `${ns}:${st}` — no tags in key
    - Dispatch tag_guarded subcluster

    NeighborhoodPool:
    - Change from `${ns}:${activeTags.slice(0, 3).join("|")}:${st}` to `${ns}:${st}` — no tags in key
    - Dispatch tag_guarded subcluster

    BridgeKey:
    - Keep `${ns}:${activeTags[0] ?? "none"}:bridge:${st}` structure
    - Will dispatch bridge_guarded subcluster (P2 adds normalizeBridgeTag)

    SdrTagPool:
    - Keep `${activeTags.join(",")}:${st}` structure
    - Will dispatch tag_sdr_guarded subcluster

    Write tests before implementation (TDD RED phase). Include a test that verifies the dispatch table entry for each coarse_key_mode against the expected strategy — this catches Rust drift if the dispatch logic changes.
  </action>
  <acceptance_criteria>
    - sdr_subcluster_tag_guarded: two records with Tanimoto=0.20 but no shared tags → NOT merged
    - sdr_subcluster_tag_guarded: two records with Tanimoto=0.20 and 1 shared tag → merged
    - Standard key for ["b","a","c","d"] tags → "ns:a,b,c:st" (only top 3)
    - TagFamily key for tags ["auth/login","config"] → alphabetically first "auth/login" (not "auth")
    - DualKey key for tags ["a","b"] → "ns:st" (no tags)
    - Subcluster dispatch table has an explicit test verifying each coarse_key_mode → strategy mapping
    - "bun run test --filter '@aura/belief'" passes all subcluster and coarse key tests
  </acceptance_criteria>
  <verify>
    <automated>cd packages/belief && bun run typecheck 2>&1 && bun run test --reporter verbose 2>&1</automated>
  </verify>
  <done>All 4 subcluster functions implemented; coarse key modes aligned with Rust; dispatch table verified; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Subcluster strategy | Controls belief formation from records |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-04 | Tampering | Tag barrier logic | mitigate | Tag barrier must not skip records when one side has no tags — verify edge case in tests |
| T-06.3-05 | Tampering | Threshold constants | mitigate | All thresholds verified by grep against Rust source, not copied from secondary documents |
| T-06.3-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
- `bun run test --filter "@aura/belief"` passes all tests
- Subcluster strategy dispatch table verified against Rust source (grep `match coarse_key_mode` or equivalent)
</verification>

<success_criteria>
- 4 SDR subcluster functions implemented (plain/tag_guarded/bridge_guarded/tag_sdr_guarded)
- All threshold values grep-verified from Rust source
- Dispatch table has explicit test coverage for each coarse_key_mode
- Standard mode truncates to top 3 tags
- TagFamily mode uses alphabetically first tag
- DualKey/NeighborhoodPool use namespace:semantic_type with tag_guarded
- All tests pass
</success_criteria>

<output>
`.planning/phases/06.3-engine-algorithm-parity/06.3-03-SUMMARY.md` when done
</output>
