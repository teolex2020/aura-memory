---
phase: 06.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contract/src/Maintenance.ts (NEW)
  - packages/contract/src/EpistemicInspection.ts (NEW)
  - packages/contract/src/EpistemicRuntime.ts (MODIFY)
  - packages/contract/src/policy/PolicyTypes.ts (MODIFY)
  - packages/contract/src/index.ts (MODIFY)
autonomous: true
requirements: [REQ-011, REQ-012]

must_haves:
  truths:
    - All maintenance types (MaintenanceConfig, PhaseTimings, LayerStability, Report types, MaintenanceReport, ConceptSurfaceMode) are exported from @aura/contract
    - All epistemic inspection types (BeliefInstabilitySummary, ContradictionCluster, PolicyLifecycleSummary, PolicyPressureArea, Reflection types, ConceptSurfaceTelemetry) are exported from @aura/contract
    - EpistemicRuntime.Interface has no maintain() method, has 18 inspection method signatures instead
    - PolicyActionKind enum exported from @aura/contract/policy/PolicyTypes.ts
    - SurfacedConcept and SurfacedPolicyHint types exported from @aura/contract
    - All new types are re-exported from @aura/contract index.ts
  artifacts:
    - path: packages/contract/src/Maintenance.ts
      provides: "All maintenance-related type definitions"
      min_lines: 200
    - path: packages/contract/src/EpistemicInspection.ts
      provides: "All epistemic inspection type definitions"
      min_lines: 150
    - path: packages/contract/src/EpistemicRuntime.ts
      provides: "Updated EpistemicRuntime.Interface with 18 inspection methods, no maintain()"
      contains: "export interface Interface"
    - path: packages/contract/src/policy/PolicyTypes.ts
      provides: "PolicyActionKind enum"
      contains: "export enum PolicyActionKind"
    - path: packages/contract/src/index.ts
      provides: "Exports for new modules"
      contains: "EpistemicInspection"
  key_links:
    - from: packages/contract/src/EpistemicRuntime.ts
      to: packages/contract/src/EpistemicInspection.ts
      via: "import type for inspection result types (BeliefInstabilitySummary, ContradictionCluster, PolicyLifecycleSummary, etc.)"
      pattern: "import type.*EpistemicInspection"
    - from: packages/contract/src/EpistemicRuntime.ts
      to: packages/contract/src/Maintenance.ts
      via: "import type for ConceptSurfaceMode"
      pattern: "import.*ConceptSurfaceMode"
    - from: packages/contract/src/index.ts
      to: packages/contract/src/Maintenance.ts
      via: "re-export"
      pattern: "export.*Maintenance"
    - from: packages/contract/src/index.ts
      to: packages/contract/src/EpistemicInspection.ts
      via: "re-export"
      pattern: "export.*EpistemicInspection"
    - from: packages/epistemic-runtime/src/EpistemicRuntime.ts (future)
      to: packages/contract/src/EpistemicRuntime.ts
      via: "implements EpistemicRuntime.Interface"
      pattern: "implements"
---

<objective>
Plan 01: Define all new contract types for MaintenanceService and EpistemicRuntime inspection.

Purpose: Establish the type foundation that all subsequent plans depend on. Every type from PROJECTION.md sections 1, 5.1, and 5.2 must be mapped exactly. This plan creates the contract types only -- no runtime logic.
Output: Maintenance.ts (NEW), EpistemicInspection.ts (NEW), updates to EpistemicRuntime.ts, PolicyTypes.ts, index.ts
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/workflows/execute-plan.md
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-epistemicruntime-maintain-maintenanceservice-rust/06.2-PROJECTION.md
@packages/contract/src/EpistemicRuntime.ts
@packages/contract/src/policy/PolicyTypes.ts
@packages/contract/src/concept/ConceptTypes.ts
@packages/contract/src/index.ts
@packages/contract/src/Belief.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Maintenance.ts with all maintenance-related types</name>
  <files>
    packages/contract/src/Maintenance.ts (NEW)
  </files>
  <read_first>
    - 06.2-PROJECTION.md sections 1.1, 1.2, 1.3, 1.4, 1.5, 1.7 (for ConceptSurfaceMode), section 2.12 (for internal phase result types)
    - packages/contract/src/belief/BeliefTypes.ts (for BeliefReport fields — do NOT change, just reference)
    - packages/contract/src/concept/ConceptTypes.ts (for ConceptReport, ConceptEngineState — reference only)
    - packages/contract/src/causal/CausalTypes.ts (for CausalReport — reference only)
    - packages/contract/src/policy/PolicyTypes.ts (for PolicyReport — reference only)
    - packages/contract/src/concept/ConceptTypes.ts (for ConceptPartitionMode, ConceptUnionMode — already exist, verify alignment)
  </read_first>
  <action>
    Create packages/contract/src/Maintenance.ts as a pure type file with NO runtime side effects. All exported interfaces are readonly (use `readonly` modifier on all fields — matching existing @aura/contract conventions).

    Per D-03, types must EXACTLY match Rust structs from PROJECTION.md (line-by-line semantic projection). Per D-07, all 14 MaintenanceService method parameters/return types are projected here even if algorithm logic is deferred.

    Define the following types in order, matching Rust field names as camelCase per TS codebase convention (snake_case only for persisted/disk-format types, which these are NOT — they are runtime report types):

    Section 1 — Config:
    - `ArchivalRule` (tag: string, maxAgeDays: number, keepRecent: number)
    - `CompletedArchivalRule` (tag: string, maxAgeDays: number)
    - `MaintenanceConfig` (decayEnabled: boolean, reflectEnabled: boolean, insightsEnabled: boolean, consolidationEnabled: boolean, synthesisEnabled: boolean, archivalEnabled: boolean, levelFixInterval: number, maxClustersPerRun: number, archivalRules: ReadonlyArray&lt;ArchivalRule&gt;, completedArchivalRules: ReadonlyArray&lt;CompletedArchivalRule&gt;, taskTag: string)
    - `defaultMaintenanceConfig` exported const with all defaults as specified in PROJECTION.md section 1.1 (especiall the `archivalRules` 10-item array and `completedArchivalRules` 2-item array with exact values from Rust)

    Section 2 — Internal phase result types (PROJECTION.md section 2.12):
    - `InitialMaintenancePhaseResult` (totalRecords: number, decay: DecayReport, reflect: ReflectReport, epistemic: EpistemicPhaseReport, insightsFound: number)
    - `DiscoveryPhaseResult` (belief: BeliefPhaseReport, concept: ConceptPhaseReport, causal: CausalPhaseReport, policy: PolicyPhaseReport, feedback: FeedbackAuditReport)
    - `PostDiscoveryPhaseResult` (consolidation: ConsolidationReport, crossConnections: number, taskReminders: ReadonlyArray&lt;string&gt;, recordsArchived: number)
    - `ConceptSurfaceCounters` (globalCalls: Ref.Ref&lt;number&gt;, namespaceCalls: Ref.Ref&lt;number&gt;, recordCalls: Ref.Ref&lt;number&gt;, conceptsReturned: Ref.Ref&lt;number&gt;, recordAnnotationsReturned: Ref.Ref&lt;number&gt;)

    Section 3 — Phase data types:
    - `PhaseTimings` (14 fields + totalMs, all number)
    - `LayerChurn` (retained: number, newCount: number, dropped: number, churn: number)
    - `LayerStability` (belief: LayerChurn, concept: LayerChurn, causal: LayerChurn, policy: LayerChurn)

    Section 4 — Report types (exact fields from PROJECTION.md):
    - `DecayReport` (decayed: number, archived: number)
    - `ReflectReport` (promoted: number, archived: number)
    - `EpistemicPhaseReport` (updatedRecords: number, totalSupportLinks: number, totalConflictLinks: number, volatileRecords: number)
    - `BeliefPhaseReport` (all 9 fields from PROJECTION.md 1.5)
    - `ConceptPhaseReport` (all 20 fields from PROJECTION.md 1.5)
    - `CausalPhaseReport` (all 22 fields from PROJECTION.md 1.5)
    - `PolicyPhaseReport` (all 8 fields from PROJECTION.md 1.5)
    - `FeedbackAuditEntry` (all 14 fields from PROJECTION.md 1.5)
    - `FeedbackAuditReport` (all 5 fields plus entries: ReadonlyArray&lt;FeedbackAuditEntry&gt;)
    - `ConsolidationReport` (nativeMerged: number, clustersFound: number, metaCreated: number)

    Section 5 — Enum:
    - `ConceptSurfaceMode` with values: Off = "Off", Inspect = "Inspect", Limited = "Limited"

    Section 6 — Hotspots and Trends:
    - `MaintenanceHotspots` (all 25 fields from PROJECTION.md 1.3)
    - `MaintenanceTrendSnapshot` (all 17 fields from PROJECTION.md 1.5)
    - `MaintenanceTrendSummary` (snapshotCount, recent, avgBeliefChurn, avgCausalRejectionRate, avgPolicySuppressionRate, avgCycleTimeMs, avgCorrectionEvents, totalCorrectionsInWindow, latestDominantPhase)

    Section 7 — Top-level report:
    - `MaintenanceReport` (all 20 fields from PROJECTION.md 1.6: timestamp, decay, reflect, epistemic, insightsFound, belief, concept, causal, policy, feedback, consolidation, crossConnections, taskReminders, recordsArchived, totalRecords, experienceInjected, timings, stability, conceptSurface, reflection, trendSummary, hotspots)

    Import Ref from "effect" only for the ConceptSurfaceCounters type. All other types are pure interfaces.

    Follow the existing `@aura/contract` pattern: NO default exports, only named exports. All interfaces use `readonly` for all fields. File ends with NO runtime code (no classes, no functions -- except the defaultMaintenanceConfig const).
  </action>
  <acceptance_criteria>
    - packages/contract/src/Maintenance.ts exists
    - Exports ArchivalRule, CompletedArchivalRule, MaintenanceConfig, defaultMaintenanceConfig, InitialMaintenancePhaseResult, DiscoveryPhaseResult, PostDiscoveryPhaseResult, ConceptSurfaceCounters, PhaseTimings, LayerChurn, LayerStability, DecayReport, ReflectReport, EpistemicPhaseReport, BeliefPhaseReport, ConceptPhaseReport, CausalPhaseReport, PolicyPhaseReport, FeedbackAuditEntry, FeedbackAuditReport, ConsolidationReport, ConceptSurfaceMode, MaintenanceHotspots, MaintenanceTrendSnapshot, MaintenanceTrendSummary, MaintenanceReport
    - All interfaces have readonly fields
    - defaultMaintenanceConfig contains exactly 10 archivalRules and 2 completedArchivalRules
    - `bun run typecheck` passes after all 3 tasks in this plan are complete
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck --filter=@aura/contract 2>&1 | head -20</automated>
  </verify>
  <done>Maintenance.ts created with ~40 type/interface/enum exports, compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create EpistemicInspection.ts with all epistemic inspection types</name>
  <files>
    packages/contract/src/EpistemicInspection.ts (NEW)
  </files>
  <read_first>
    - 06.2-PROJECTION.md sections 1.6 (all specialty types), 1.8 (SurfacedConcept, SurfacedPolicyHint)
    - packages/contract/src/policy/PolicyTypes.ts (for context on existing policy types)
    - packages/contract/src/Maintenance.ts (after Task 1 completes)
  </read_first>
  <action>
    Create packages/contract/src/EpistemicInspection.ts as a pure type file.

    Per D-09, all inspection types align 1:1 with Rust upstream types (epistemic_runtime.rs, background_brain.rs). The SurfacedConcept and SurfacedPolicyHint types defined here are shared contract types consumed by @aura/concept and @aura/policy surface functions (per D-11 delegation pattern).

    Define ALL the following types — exact fields and shapes from PROJECTION.md section 1.6 and 1.8:

    Inspection result types:
    - `BeliefVolatilityBands` (low: number, medium: number, high: number)
    - `BeliefInstabilitySummary` (totalBeliefs, resolved, unresolved, singleton, empty, contradictionClusterCount, highVolatilityCount, lowStabilityCount, avgVolatility, avgStability, volatilityBands: BeliefVolatilityBands)
    - `ContradictionCluster` (id, namespace, beliefIds: ReadonlyArray&lt;string&gt;, beliefKeys: ReadonlyArray&lt;string&gt;, recordIds: ReadonlyArray&lt;string&gt;, sharedTags: ReadonlyArray&lt;string&gt;, unresolvedBeliefCount, highVolatilityBeliefCount, avgVolatility, avgStability, totalConflictMass, maxConflictMass)
    - `PolicyActionSummary` (actionKind: string, totalHints, stableHints, candidateHints, suppressedHints, rejectedHints, avgPolicyStrength, avgRiskScore)
    - `PolicyDomainSummary` (namespace, domain, totalHints, activeHints, stableHints, candidateHints, suppressedHints, rejectedHints, avgPolicyStrength, avgRiskScore, advisoryPressure)
    - `PolicyPressureArea` (namespace, domain, advisoryPressure, activeHints, suppressedHints, rejectedHints, strongestHintId, strongestActionKind, strongestPolicyStrength)
    - `PolicyLifecycleSummary` (totalHints, activeHints, stableHints, candidateHints, suppressedHints, rejectedHints, avgPolicyStrength, avgRiskScore, actionSummaries: ReadonlyArray&lt;PolicyActionSummary&gt;, domainSummaries: ReadonlyArray&lt;PolicyDomainSummary&gt;)
    - `ConceptSurfaceTelemetry` (mode: string, surfacedConceptsAvailable, surfacedNamespaces, globalCallsSinceLastCycle, namespaceCallsSinceLastCycle, recordCallsSinceLastCycle, conceptsReturnedSinceLastCycle, recordAnnotationsReturnedSinceLastCycle)

    Reflection types:
    - `ReflectionFinding` (kind, namespace, title, detail, relatedIds: ReadonlyArray&lt;string&gt;, score, severity: "high" | "medium" | "low")
    - `ReflectionJobReport` (jobsRun, blockerFindings, contradictionFindings, trendFindings, totalFindings, capped)
    - `ReflectionSummary` (timestamp: string, digest: string, dominantPhase: string, report: ReflectionJobReport, findings: ReadonlyArray&lt;ReflectionFinding&gt;)
    - `ReflectionKindSummary` (kind: string, count, highSeverityCount, avgScore)
    - `ReflectionDigest` (summaryCount, totalFindings, highSeverityFindings, latestTimestamp: string, latestDominantPhase: string, kinds: ReadonlyArray&lt;ReflectionKindSummary&gt;, namespaces: ReadonlyArray&lt;string&gt;, topFindings: ReadonlyArray&lt;ReflectionFinding&gt;)

    Surface types (shared contract types, imported by @aura/concept and @aura/policy):
    - `ConceptSurfaceMode` -- DO NOT redefine here, import from ./Maintenance.ts
    - `SurfacedConcept` (id: string, key: string, state: string, namespace: string, abstractionScore: number, beliefCount: number, recordCount: number, coreTerms: ReadonlyArray&lt;string&gt;, recordIds: ReadonlyArray&lt;string&gt;)
    - `SurfacedPolicyHint` (id: string, state: string, actionKind: string, namespace: string, domain: string, recommendation: string, policyStrength: number, riskScore: number, triggerCausalIds: ReadonlyArray&lt;string&gt;)

    All fields readonly. Named exports only. No runtime code.
  </acceptance_criteria>
    - packages/contract/src/EpistemicInspection.ts exists
    - Exports all 16+ types listed above
    - `bun run typecheck --filter=@aura/contract` passes
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck --filter=@aura/contract 2>&1 | head -20</automated>
  </verify>
  <done>EpistemicInspection.ts created with all 16+ type exports, compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 3: Update EpistemicRuntime.ts Interface, PolicyTypes.ts, and index.ts exports</name>
  <files>
    packages/contract/src/EpistemicRuntime.ts (MODIFY)
    packages/contract/src/policy/PolicyTypes.ts (MODIFY)
    packages/contract/src/index.ts (MODIFY)
  </files>
  <read_first>
    - Current packages/contract/src/EpistemicRuntime.ts (entire file)
    - Current packages/contract/src/policy/PolicyTypes.ts (entire file)
    - Current packages/contract/src/index.ts (entire file)
    - Current packages/contract/src/Belief.ts (for the namespace.Interface pattern reference)
    - 06.2-PROJECTION.md sections 3.1, 3.2 (for new Interface), section 1.7 (for PolicyActionKind), section 5.2 (for files to modify)
    - packages/contract/src/Maintenance.ts (after Task 1, for ConceptSurfaceMode import)
    - packages/contract/src/EpistemicInspection.ts (after Task 2, for inspection result type imports)
    - packages/contract/src/concept/ConceptTypes.ts (for ConceptCandidate type)
    - packages/contract/src/causal/CausalTypes.ts (for CausalPattern type)
    - packages/contract/src/policy/PolicyTypes.ts (for PolicyHint, PolicyState types)
  </read_first>
  <action>
    Modify three files. Per D-14, type projection precedes implementation — this plan covers all contract changes before any runtime work. Per D-02, EpistemicRuntime Interface becomes pure read-only (no maintain()).

    **File 1: packages/contract/src/EpistemicRuntime.ts**
    - REPLACE the entire file content
    - FOLLOW the namespace.Interface pattern from Belief.ts (export namespace EpistemicRuntime { export interface Interface { ... } })
    - Remove: `type EpistemicReport`, remove `maintain()`, remove `get_beliefs()` / `get_concepts()` / `get_causal_patterns()` / `get_policy_hints()` (old snake_case names)
    - Remove stub methods: `get_surfaced_concepts`, `get_surfaced_policy_hints`
    - The new Interface name is `Interface` (inside namespace), NOT `EpistemicRuntimeImpl`
    - ADD all 18 new inspection methods from PROJECTION.md section 3.2:
      - Belief layer (6): getBeliefs, getBeliefForRecord, getHighVolatilityBeliefs, getLowStabilityBeliefs, getBeliefInstabilitySummary, getContradictionClusters
      - Concept layer (4): getConcepts, getSurfacedConcepts, getSurfacedConceptsForNamespace, getSurfacedConceptsForRecord
      - Causal layer (1): getCausalPatterns
      - Policy layer (7): getPolicyHints, getSuppressedPolicyHints, getRejectedPolicyHints, getPolicyLifecycleSummary, getPolicyPressureReport, getSurfacedPolicyHints, getSurfacedPolicyHintsForNamespace
    - Imports needed:
      - import type { ConceptSurfaceMode } from "./Maintenance"
      - import type { SurfacedConcept, SurfacedPolicyHint, BeliefInstabilitySummary, ContradictionCluster, PolicyLifecycleSummary, PolicyPressureArea } from "./EpistemicInspection"
      - import type { Record as AuraRecord } from "./record/Record"
      - import type { Belief, BeliefEngine } from "./Belief"
      - import type { ConceptCandidate, ConceptEngine } from "./Concept"
      - import type { CausalPattern, CausalEngine } from "./Causal"
      - import type { PolicyHint, PolicyEngine } from "./Policy"
      - import type { Effect } from "effect"
    - Method signatures exactly from PROJECTION.md section 3.2 (with camelCase params and Effect return types)
    - The `getContradictionClusters` method signature takes `records: ReadonlyMap&lt;string, AuraRecord&gt;` as FIRST parameter (per Rust's records field access pattern, see RESEARCH.md Open Question #3)
    - Export `EpistemicRuntime` class (same Tag pattern, now with `EpistemicRuntime.Interface` as the type parameter)

    **File 2: packages/contract/src/policy/PolicyTypes.ts**
    - Add `PolicyActionKind` enum with values: Prefer = "prefer", Recommend = "recommend", VerifyFirst = "verify_first", Avoid = "avoid", Warn = "warn"
    - Add these fields to `PolicyHint` type: actionKind: string, policyStrength: number, riskScore: number, namespace: string, domain: string
    - Add these fields to `PolicyEngineState` type: key_index: Readonly&lt;Record&lt;string, string&gt;&gt;

    **File 3: packages/contract/src/index.ts**
    - Add: `export * from "./Maintenance"`
    - Add: `export * from "./EpistemicInspection"`

    Verify compilation: `bun run typecheck --filter=@aura/contract` must pass.
    Verify that no other package breaks (the EpistemicRuntime class tag is the same, so service lookups still work).
  </acceptance_criteria>
    - packages/contract/src/EpistemicRuntime.ts uses namespace.Interface pattern (export namespace EpistemicRuntime { export interface Interface { ... } })
    - EpistemicRuntime.ts has NO maintain() method, has exactly 18 inspection methods
    - packages/contract/src/policy/PolicyTypes.ts exports PolicyActionKind enum
    - PolicyHint type has actionKind, policyStrength, riskScore, namespace, domain fields
    - packages/contract/src/index.ts exports both ./Maintenance and ./EpistemicInspection
    - `bun run typecheck` passes across the entire project
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck 2>&1 | head -30</automated>
    <human-check>Confirm packages/epistemic-runtime has NO type errors (expected: EpistemicRuntimeImpl needs updating — THAT'S for Plan 04)</human-check>
  </verify>
  <done>All contract types defined, EpistemicRuntime interface migrated to namespace.Interface pattern, PolicyHint extended, PolicyActionKind added, index.ts exports new modules</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| compile-time type checking | All types in this plan are evaluated at compile time only; no runtime data crosses any boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-01 | Spoofing | EpistemicRuntime.Interface type parameter | mitigate | namespace.Interface pattern ensures compile-time type safety for service lookups |
| T-06.2-02 | Tampering | Type field alignment | mitigate | All type fields marked readonly per codebase convention |
| T-06.2-SC | Tampering | npm package integrity | accept | No new packages introduced; all deps existing project dependencies |
</threat_model>

<verification>
- `bun run typecheck` passes after Task 3
- grep confirms no `maintain` in EpistemicRuntime.ts contract
- grep confirms 18 method signatures in EpistemicRuntime.Interface
- grep confirms PolicyActionKind, SurfacedConcept, SurfacedPolicyHint are exported
</verification>

<success_criteria>
1. All maintenance and epistemic inspection types defined and exported
2. EpistemicRuntime.Interface migrated to namespace.Interface pattern with 18 methods
3. PolicyHint extended with actionKind, policyStrength, riskScore, namespace, domain
4. PolicyActionKind enum defined
5. index.ts re-exports both new modules
6. Full project typecheck passes (with expected error in epistemic-runtime package — covered in Plan 04)
</success_criteria>

<output>
Create `.planning/phases/06.2-epistemicruntime-maintain-maintenanceservice-rust/06.2-01-SUMMARY.md` when done
</output>
