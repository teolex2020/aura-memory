---
phase: 06.2
plan: GAP-01
type: execute
wave: 4
depends_on: []
files_modified:
  - packages/policy/src/Surface.ts (MODIFY)
  - packages/policy/src/Surface.test.ts (MODIFY)
  - packages/epistemic-runtime/src/EpistemicRuntime.ts (MODIFY)
autonomous: true
requirements: [REQ-011]
user_setup: []

must_haves:
  truths:
    - "packages/policy/src/Surface.ts imports SurfacedPolicyHint from @aura/contract/EpistemicInspection.ts instead of defining a local type"
    - "packages/policy/src/Surface.ts imports PolicyActionKind enum from @aura/contract/policy/PolicyTypes.ts instead of a local string union"
    - "packages/policy/src/Surface.ts imports PolicyState enum from @aura/contract/policy/PolicyTypes.ts instead of a local string union"
    - "packages/policy/src/Surface.ts still exports local PolicyHint and PolicyEngine interfaces (internal engine types)"
    - "packages/policy/src/Surface.test.ts compiles and all tests pass after type migration"
    - "packages/epistemic-runtime/src/EpistemicRuntime.ts imports PolicyState from @aura/contract instead of @aura/policy"
  artifacts:
    - path: packages/policy/src/Surface.ts
      provides: "Surface functions using contract types for SurfacedPolicyHint, PolicyActionKind, PolicyState"
      contains: "import.*SurfacedPolicyHint.*from.*EpistemicInspection"
    - path: packages/policy/src/Surface.test.ts
      provides: "Updated tests passing with new contract type imports"
      contains: "PolicyActionKind"
    - path: packages/epistemic-runtime/src/EpistemicRuntime.ts
      provides: "PolicyState import from @aura/contract instead of @aura/policy"
      pattern: "import.*PolicyState.*from.*@aura/contract"
  key_links:
    - from: packages/policy/src/Surface.ts
      to: packages/contract/src/EpistemicInspection.ts
      via: "import { type SurfacedPolicyHint } from '@aura/contract'"
      pattern: "SurfacedPolicyHint.*@aura/contract"
    - from: packages/policy/src/Surface.ts
      to: packages/contract/src/policy/PolicyTypes.ts
      via: "import { PolicyActionKind, PolicyState } from '@aura/contract'"
      pattern: "PolicyActionKind.*@aura/contract"
---

<objective>
Replace local type definitions in packages/policy/src/Surface.ts with imports from @aura/contract, closing the verification gap where surface functions defined local PolicyActionKind, PolicyState, and SurfacedPolicyHint types instead of using the canonical contract types.

Purpose: Wire the direct key links between policy/Surface.ts and contract types that VERIFICATION.md identified as NOT_WIRED. The functions work via EpistemicRuntime adapter bridge, but the contract type imports must exist for architectural consistency.
Output: Updated Surface.ts, Surface.test.ts, and EpistemicRuntime.ts with proper contract type imports.
Decisions: D-11 (surface functions in @aura/policy), D-17 (surface tests)
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/workflows/execute-plan.md
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-epistemicruntime-maintain-maintenanceservice-rust/06.2-VERIFICATION.md
@packages/contract/src/EpistemicInspection.ts
@packages/contract/src/policy/PolicyTypes.ts
@packages/policy/src/Surface.ts
@packages/policy/src/Surface.test.ts
@packages/epistemic-runtime/src/EpistemicRuntime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace local types with contract imports in packages/policy/src/Surface.ts</name>
  <files>
    packages/policy/src/Surface.ts (MODIFY)
  </files>
  <read_first>
    - packages/contract/src/EpistemicInspection.ts (SurfacedPolicyHint type — has id, state, actionKind, namespace, domain, recommendation, policyStrength, riskScore, triggerCausalIds only; no confidence, triggerConceptIds, triggerBeliefIds, supportingRecordIds)
    - packages/contract/src/policy/PolicyTypes.ts (PolicyActionKind enum with values Prefer="prefer", Recommend="recommend", VerifyFirst="verify_first", Avoid="avoid", Warn="warn"; PolicyState enum with values Candidate="Candidate", Stable="Stable", Suppressed="Suppressed", Rejected="Rejected")
    - packages/policy/src/Surface.ts (CURRENT — full 267-line file for reference)
  </read_first>
  <action>
    Modify packages/policy/src/Surface.ts to replace the three local type definitions with contract imports. Keep the local PolicyHint and PolicyEngine interfaces unchanged.

    **Step 1 — Add imports from @aura/contract (replace the current Effect-only import):**

    At the top of the file, replace the single import:
    ```typescript
    import { Effect } from "effect"
    ```
    With:
    ```typescript
    import { Effect } from "effect"
    import { PolicyActionKind, PolicyState, type SurfacedPolicyHint } from "@aura/contract"
    ```

    **Step 2 — Remove the three local type definitions (lines 29-34):**
    - Remove `export type PolicyActionKind = "Prefer" | "Recommend" | "VerifyFirst" | "Avoid" | "Warn"`
    - Remove `export type PolicyState = "Candidate" | "Stable" | "Suppressed" | "Rejected"`
    - Keep the `ACTION_KIND_PRIORITY` constant but update its keys to use the enum's string values (lowercase) instead of the capitalized strings

    **Step 3 — Remove the local `interface SurfacedPolicyHint` definition (lines 99-126):**
    - Remove the entire local `export interface SurfacedPolicyHint { ... }` block (lines 99-126)
    - The type is now imported from `@aura/contract`

    **Step 4 — Update internal PolicyHint interface to use imported enums:**
    - Change `readonly actionKind: PolicyActionKind` — the enum type has lowercase values, so this changes the accepted values from capitalized to lowercase
    - Change `readonly state: PolicyState` — values match (Stable="Stable", Candidate="Candidate", etc.)
    - The interface declaration stays local (it's internal to the package)

    **Step 5 — Update ACTION_KIND_PRIORITY keys to use enum values:**
    The current map uses capitalized keys:
    ```typescript
    const ACTION_KIND_PRIORITY: Record<PolicyActionKind, number> = {
      Avoid: 0, Warn: 1, VerifyFirst: 2, Recommend: 3, Prefer: 4,
    }
    ```
    Replace with computed property names using the enum:
    ```typescript
    const ACTION_KIND_PRIORITY: Record<PolicyActionKind, number> = {
      [PolicyActionKind.Avoid]: 0,
      [PolicyActionKind.Warn]: 1,
      [PolicyActionKind.VerifyFirst]: 2,
      [PolicyActionKind.Recommend]: 3,
      [PolicyActionKind.Prefer]: 4,
    }
    ```

    **Step 6 — Remove `actionKindString()` helper (lines 44-53):**
    The contract's PolicyActionKind enum already has lowercase runtime values, so the mapping is identity. Remove the function. The `toSurfaced()` function should assign `hint.actionKind` directly to the output field.

    **Step 7 — Update `stateString()` helper (lines 56-62):**
    Replace the switch statement with a simpler mapping using `PolicyState` enum values:
    ```typescript
    function stateString(state: PolicyState): SurfacedPolicyHint["state"] {
      if (state === PolicyState.Stable) return "stable"
      if (state === PolicyState.Candidate) return "candidate"
      // Suppressed/Rejected are filtered out by isHintEligible, so default is safe
      return "candidate"
    }
    ```
    Note: the return type is `SurfacedPolicyHint["state"]` which is `string` (the contract type has `state: string`).

    **Step 8 — Update `toSurfaced()` function (lines 156-175):**
    Remove the extra fields that don't exist in the contract's SurfacedPolicyHint:
    - Remove `confidence` field
    - Remove `triggerConceptIds` field
    - Remove `triggerBeliefIds` field
    - Remove `supportingRecordIds` field
    - Change `actionKind: actionKindString(hint.actionKind)` to `actionKind: hint.actionKind` (no conversion needed)
    Keep: id, state (via stateString), actionKind, namespace, domain, recommendation (truncated), policyStrength, riskScore, triggerCausalIds.

    **Step 9 — Add re-exports of imported types so consumers of @aura/policy can still find them:**
    At the bottom of the file (or in the import/export section), add:
    ```typescript
    export { PolicyActionKind, PolicyState } from "@aura/contract"
    export type { SurfacedPolicyHint } from "@aura/contract"
    ```

    After changes, verify compilation:
    ```bash
    bun run typecheck --filter=@aura/policy
    ```
  </action>
  <acceptance_criteria>
    - No local `type PolicyActionKind` or `type PolicyState` definitions in Surface.ts
    - No local `interface SurfacedPolicyHint` definition in Surface.ts
    - `SurfacedPolicyHint` imported from `@aura/contract` (EpistemicInspection.ts)
    - `PolicyActionKind` and `PolicyState` imported from `@aura/contract` (policy/PolicyTypes.ts)
    - `ACTION_KIND_PRIORITY` uses computed property names with `PolicyActionKind.Avoid` etc.
    - `actionKindString()` removed; `toSurfaced()` assigns `hint.actionKind` directly
    - `toSurfaced()` output matches contract SurfacedPolicyHint (no extra fields: confidence, triggerConceptIds, triggerBeliefIds, supportingRecordIds)
    - Re-exports of PolicyActionKind, PolicyState, SurfacedPolicyHint present for downstream consumers
    - `bun run typecheck --filter=@aura/policy` passes
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck --filter=@aura/policy 2>&1 | tail -20</automated>
  </verify>
  <done>Surface.ts uses contract types; typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Update packages/policy/src/Surface.test.ts to match contract types</name>
  <files>
    packages/policy/src/Surface.test.ts (MODIFY)
  </files>
  <read_first>
    - packages/policy/src/Surface.test.ts (CURRENT — full 246-line file)
    - packages/policy/src/Surface.ts (AFTER Task 1 modifications)
    - packages/contract/src/policy/PolicyTypes.ts (PolicyActionKind enum with lowercase values: prefer, recommend, verify_first, avoid, warn)
  </read_first>
  <action>
    Modify Surface.test.ts so all test code compiles and passes with the new contract types.

    **Step 1 — Update the `makeHint` helper (lines 15-47):**
    The `action: PolicyActionKind` parameter now receives the enum type. Change test callers from capitalized string literals to enum member access:
    - `"Prefer"` becomes `PolicyActionKind.Prefer`
    - `"Recommend"` becomes `PolicyActionKind.Recommend`
    - `"VerifyFirst"` becomes `PolicyActionKind.VerifyFirst`
    - `"Avoid"` becomes `PolicyActionKind.Avoid`
    - `"Warn"` becomes `PolicyActionKind.Warn`
    
    Similarly for state: `"Stable"` and `"Candidate"` already match enum values, but use the enum for consistency:
    - `"Stable"` becomes `PolicyState.Stable`
    - `"Candidate"` becomes `PolicyState.Candidate`
    - `"Suppressed"` becomes `PolicyState.Suppressed`
    - `"Rejected"` becomes `PolicyState.Rejected`

    **Step 2 — Update test assertions (lines 78-81) that check surfaced hint state/actionKind:**
    The output `SurfacedPolicyHint.state` is now `string` (not `"stable" | "candidate"`), and `actionKind` is `string` (not `"prefer" | ...`). The assertion values stay the same since the enum produces the same runtime strings (e.g., `PolicyActionKind.Prefer` has runtime value `"prefer"`, and the output `actionKind` from the surface function is `"prefer"` which equals `PolicyActionKind.Prefer`). But double-check that:
    - `surfaced[0]!.state` === `"stable"` (for Stable hints)
    - `surfaced[0]!.state` === `"candidate"` (for Candidate hints)
    - `surfaced[0]!.actionKind` === `"prefer"` (for Prefer hints)
    - `surfaced[0]!.actionKind` === `"recommend"` (for Recommend hints)
    
    The contract's SurfacedPolicyHint.state is `"stable" | "candidate"` mapped via `stateString()`, which hasn't changed its output values.

    **Step 3 — Remove assertions for fields that no longer exist on SurfacedPolicyHint (lines 215-218):**
    The contract SurfacedPolicyHint does NOT have `triggerConceptIds`, `triggerBeliefIds`, or `supportingRecordIds` fields. Update test 11 ("surfaced hints have full provenance", lines 203-219):
    - Remove the three assertions for `.triggerConceptIds`, `.triggerBeliefIds`, `.supportingRecordIds`
    - Keep the assertion for `.triggerCausalIds` (which IS a field on the contract type)

    **Step 4 — Run tests to verify:**
    ```bash
    bun vitest run packages/policy/src/Surface.test.ts
    ```
    All 12 tests must pass. If any fail, fix the assertion values to match the new contract SurfacedPolicyHint shape.
  </action>
  <acceptance_criteria>
    - `makeHint` helper uses `PolicyActionKind.Prefer` etc. instead of capitalized string `"Prefer"`
    - `makeHint` helper uses `PolicyState.Stable` etc. instead of string `"Stable"`
    - Test assertions for non-existent fields (triggerConceptIds, triggerBeliefIds, supportingRecordIds) removed
    - `triggerCausalIds` assertion remains
    - `bun vitest run packages/policy/src/Surface.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <verify>
    <automated>bun vitest run packages/policy/src/Surface.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>Surface.test.ts passes with all 12 tests</done>
</task>

<task type="auto">
  <name>Task 3: Update EpistemicRuntime.ts imports from @aura/policy for removed exports</name>
  <files>
    packages/epistemic-runtime/src/EpistemicRuntime.ts (MODIFY)
  </files>
  <read_first>
    - packages/epistemic-runtime/src/EpistemicRuntime.ts (line 25: imports `PolicyState as SurfacePolicyState` from `@aura/policy`)
    - packages/policy/src/Surface.ts (AFTER Task 1 — PolicyState is no longer a local type, it's re-exported from @aura/contract)
    - packages/contract/src/policy/PolicyTypes.ts (PolicyState enum export path)
  </read_first>
  <action>
    The EpistemicRuntime currently imports `type { PolicyState as SurfacePolicyState } from "@aura/policy"` (line 25). After the Surface.ts changes, `PolicyState` is no longer defined in Surface.ts but is re-exported from `@aura/contract`. The re-export from `@aura/policy` should still work since Surface.ts adds `export { PolicyState } from "@aura/contract"` — but it creates a transitive dependency chain that is fragile.

    Fix by importing from the canonical source instead. Update line 25 in EpistemicRuntime.ts:

    Before:
    ```typescript
    import type { PolicyHint as SurfacePolicyHint } from "@aura/policy"
    import type { PolicyEngine as SurfacePolicyEngine } from "@aura/policy"
    import type { PolicyState as SurfacePolicyState } from "@aura/policy"
    ```

    After:
    ```typescript
    import type { PolicyHint as SurfacePolicyHint } from "@aura/policy"
    import type { PolicyEngine as SurfacePolicyEngine } from "@aura/policy"
    import { PolicyState as SurfacePolicyState } from "@aura/contract"
    ```
    Note: `PolicyState` is a regular enum (not just a type), so use a regular import (not `import type`). But since the code only uses it as a type (in the `stateMap` Record), `import type` works. Either form is acceptable — use whichever compiles cleanly.

    Also check if `SurfacePolicyState` is actually used in the file. If the import is unused after the changes, remove it. Search for `SurfacePolicyState` references in the file after the change.

    Verify compilation:
    ```bash
    bun run typecheck --filter=@aura/epistemic-runtime
    ```
  </action>
  <acceptance_criteria>
    - EpistemicRuntime.ts no longer imports `PolicyState` from `@aura/policy`
    - EpistemicRuntime.ts imports `PolicyState` from `@aura/contract` instead
    - No unused imports after the change
    - `bun run typecheck --filter=@aura/epistemic-runtime` passes
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck --filter=@aura/epistemic-runtime 2>&1 | tail -20</automated>
  </verify>
  <done>EpistemicRuntime.ts imports PolicyState from @aura/contract; typecheck passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Surface.ts type imports → contract types | Imported types must match the consumed type shape; the contract SurfacedPolicyHint is narrower than the old local type |
| Re-export chain: @aura/policy → @aura/contract | Transitive re-exports must be unambiguous |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-GAP01-01 | Tampering | Contract SurfacedPolicyHint narrower than local type | mitigate | Remove extra fields from toSurfaced() mapping; test assertions for removed fields must be deleted |
| T-06.2-GAP01-02 | Tampering | PolicyActionKind enum values (lowercase) differ from old type (capitalized) | mitigate | Update ACTION_KIND_PRIORITY keys, remove actionKindString, update test enum references |
| T-06.2-GAP01-03 | Tampering | EpistemicRuntime import from @aura/policy for removed export | mitigate | Import PolicyState from @aura/contract directly instead |
| T-06.2-GAP01-SC | Tampering | npm package installs | accept | No new packages |
</threat_model>

<verification>
- grep: `grep -c "type PolicyActionKind" packages/policy/src/Surface.ts` must output 0 (no local def)
- grep: `grep -c "type PolicyState" packages/policy/src/Surface.ts` must output 0 (no local def)
- grep: `grep -c "interface SurfacedPolicyHint" packages/policy/src/Surface.ts` must output 0 (no local def)
- grep: `grep "import.*SurfacedPolicyHint.*from" packages/policy/src/Surface.ts` must match import from @aura/contract
- `bun run typecheck --filter=@aura/policy` passes
- `bun vitest run packages/policy/src/Surface.test.ts` passes
- `bun run typecheck --filter=@aura/epistemic-runtime` passes
</verification>

<success_criteria>
1. Surface.ts uses contract types for SurfacedPolicyHint, PolicyActionKind, PolicyState
2. Local PolicyHint and PolicyEngine interfaces remain (internal engine types)
3. All 12 Surface tests pass
4. EpistemicRuntime.ts imports PolicyState from @aura/contract
5. `bun run typecheck` passes for @aura/policy and @aura/epistemic-runtime
</success_criteria>

<output>
Create `.planning/phases/06.2-epistemicruntime-maintain-maintenanceservice-rust/06.2-GAP-01-SUMMARY.md` when done
</output>
