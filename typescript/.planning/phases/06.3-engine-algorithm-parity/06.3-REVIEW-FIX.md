---
phase: "06.3"
fixed_at: "2026-05-29T08:30:00Z"
review_path: ".planning/phases/06.3-engine-algorithm-parity/06.3-REVIEW.md"
iteration: 2
findings_in_scope: 22
fixed: 20
skipped: 2
status: partial
fix_scope: all
---

# Phase 06.3: Code Review Fix Report

**Fixed at:** 2026-05-29T08:30:00Z
**Source review:** `.planning/phases/06.3-engine-algorithm-parity/06.3-REVIEW.md`
**Iteration:** 2 (re-fix after follow-up review)

**Summary:**
- Findings in scope: 22 (all: 5 Critical + 7 Warning + 10 Info)
- Fixed: 20
- Skipped: 2
- Status: partial (2 refactoring-level Info findings skipped)

## Fixed Issues

### CR-01: ConceptEngineImpl missing `implements ConceptEngine.Interface`

**Files modified:** `packages/concept/src/ConceptEngine.ts`
**Commit:** `844dad3`
**Applied fix:** Added `implements ConceptEngine.Interface` to `ConceptEngineImpl` class declaration (`ConceptEngine` Tag is already imported as value, providing the namespace).

### CR-02: PolicyEngine Surface.ts local type definitions structurally incompatible with contract PolicyHint

**Files modified:** `packages/policy/src/Surface.ts`, `packages/epistemic-runtime/src/EpistemicRuntime.ts`
**Commit:** `4c9436a`
**Applied fix:** Fixed `toSurfacePolicyHint` conversion in EpistemicRuntime.ts to populate `triggerCausalIds`, `supportingRecordIds`, and `causeRecordIds` from contract fields (`cause_record_ids`, `effect_keys`). Updated key mapping to use `cause_key`. Marked local interfaces as `@deprecated`.

### CR-03: PolicyEngine Surface.ts isHintEligible uses raw string comparisons instead of PolicyState enum

**Files modified:** `packages/policy/src/Surface.ts`
**Commit:** `cac9df0`
**Applied fix:** Replaced `"Stable"`, `"Candidate"`, `"Suppressed"`, `"Rejected"` with `PolicyState.Stable`, `PolicyState.Candidate`, etc. in switch cases and sort tiebreaks.

### CR-04: Unsafe `CognitiveRecord as unknown as AuraRecord` cast

**Files modified:** `packages/storage/src/CognitiveRecord.ts`, `packages/storage/src/RecallView.ts`, `packages/core/src/Aura.ts`
**Commit:** `f0dd2f9` (combined with CR-05)
**Applied fix:** Resolved by CR-05. CognitiveRecord is now an alias for AuraRecord; normalizeCognitiveRecord returns fully-initialized AuraRecord.

### CR-05: `CognitiveRecord` is a duplicate of contract `Record`

**Files modified:** `packages/storage/src/CognitiveRecord.ts`, `packages/storage/src/RecallView.ts`, `packages/core/src/Aura.ts`
**Commit:** `f0dd2f9`
**Applied fix:** normalizeCognitiveRecord returns AuraRecord with 22 fully-defaulted fields. loadCognitiveRecords returns Map<string, AuraRecord>. CognitiveRecord deprecated as alias. RecallView functions updated to use AuraRecord.

### WR-01: `deprecate_belief` uses non-deterministic `Date.now()` instead of Effect Clock

**Files modified:** `packages/belief/src/BeliefEngine.ts`
**Commit:** `42b3252`
**Applied fix:** Converted from `Effect.sync()` to `Effect.gen()`, using `yield* Clock.nowSeconds()` for deterministic time.

### WR-02: Contract `apply_layer_feedback` return type is `Effect.Effect<unknown>`

**Files modified:** `packages/contract/src/Belief.ts`, `packages/belief/src/BeliefEngine.ts`
**Commit:** `e6d05e5` (combined with WR-04/05)
**Applied fix:** Changed contract return type to `Effect.Effect<FeedbackAuditReport>` (imported from contract/Maintenance.ts). Deprecated local `BeliefFeedbackReport` as alias.

### WR-03: `CausalStoreFile.empty_engine()` uses raw string `as` casts

**Files modified:** `packages/storage/src/CausalStoreFile.ts`, `packages/storage/src/CausalStoreFile.test.ts`
**Commit:** `0afac3b`
**Applied fix:** Replaced `"Standard" as CausalDiscoveryMode` with `CausalDiscoveryMode.Standard` etc. Converted type-only imports to value imports.

### WR-04: `with_coarse_key_mode` and `claim_key_with_mode` accept `mode: unknown`

**Files modified:** `packages/contract/src/belief/BeliefTypes.ts`, `packages/contract/src/Belief.ts`, `packages/belief/src/BeliefEngine.ts`
**Commit:** `e6d05e5` (combined with WR-02/05)
**Applied fix:** Changed params from `unknown` to `CoarseKeyMode` in contract and implementation. Removed runtime type guard.

### WR-05: `CoarseKeyMode` enum defined in implementation, not in contract

**Files modified:** `packages/contract/src/belief/BeliefTypes.ts`, `packages/belief/src/BeliefEngine.ts`
**Commit:** `e6d05e5` (combined with WR-02/04)
**Applied fix:** Moved CoarseKeyMode to BeliefTypes.ts. Replaced local enum with re-export for backward compat.

### WR-06: Redundant `as unknown as` casts in BeliefEngine.ts helpers

**Files modified:** `packages/belief/src/BeliefEngine.ts`
**Commit:** `6dcbed2`
**Applied fix:** Replaced `(rec as unknown as { confidence?: unknown }).confidence` with `rec.confidence` in confidenceOf/supportMassOf/conflictMassOf.

### WR-07: Redundant `as unknown as` in PolicyEngine.ts PolicyActionKind cast

**Files modified:** `packages/policy/src/PolicyEngine.ts`
**Commit:** `8b7aed5`
**Applied fix:** Removed self-cast: actionKind is already PolicyActionKind from mapActionKind().

### IN-01: Stale STUB/TDD comments in ConceptEngine.ts

**Files modified:** `packages/concept/src/ConceptEngine.ts`
**Commit:** `1023546`
**Applied fix:** Replaced misleading STUB comment with accurate description of full implementation.

### IN-02: Stale STUB comment in Concept Surface.ts

**Files modified:** `packages/concept/src/Surface.ts`
**Commit:** `82f01a3`
**Applied fix:** Removed "Returns empty array" comment. computeSurfaceConcepts is fully implemented.

### IN-03/04/05: Test file uses deprecated `*Impl` type aliases

**Files modified:** `packages/epistemic-runtime/src/EpistemicRuntime.test.ts`
**Commit:** `f72b81c`
**Applied fix:** Replaced ConceptEngineImpl/CausalEngineImpl/PolicyEngineImpl with ConceptEngine.Interface/CausalEngine.Interface/PolicyEngine.Interface.

### IN-06: Redundant sort in denseBackoffGroupKey

**Files modified:** `packages/belief/src/BeliefEngine.ts`
**Commit:** `47e38db`
**Applied fix:** Added comment explaining defensive sort (ensures determinism even if upstream input order changes).

### IN-09: Redundant `as unknown as` in Aura.ts toRecordLike

**Files modified:** `packages/core/src/Aura.ts`
**Commit:** `f0dd2f9` (resolved by CR-05)
**Applied fix:** After CR-05, toRecordLike takes AuraRecord which lacks index signature, so the cast is now necessary.

### IN-10: Repeated `as unknown as _PhaseTimings` pattern in MaintenanceService

**Files modified:** `packages/core/src/MaintenanceService.ts`
**Commit:** `48241df`
**Applied fix:** Extracted `toMutable<T>()` helper; replaced all 9 cast occurrences.

## Skipped Issues

### IN-07: Five separate Union-Find implementations

**File:** `packages/belief/src/BeliefEngine.ts`, `packages/concept/src/ConceptEngine.ts`
**Reason:** Refactoring-level change. Extracting shared UnionFind class requires restructuring 6 functions across 2 files. Each copy may have unique behavior. Not safe for automated fix.

### IN-08: Duplicate polarity signal counting functions

**File:** `packages/causal/src/CausalEngine.ts`, `packages/policy/src/PolicyEngine.ts`
**Reason:** Refactoring-level change. The two functions have deliberately different keyword lists (CausalEngine includes "noise"/"review" in negative keywords). Need to verify whether this difference is intentional. Not safe for automated fix.

---

_Fixed: 2026-05-29T08:30:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 2_
