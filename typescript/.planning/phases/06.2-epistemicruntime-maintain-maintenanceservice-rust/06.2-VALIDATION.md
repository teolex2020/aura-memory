---
phase: 06.2
slug: epistemicruntime-maintain-maintenanceservice-rust
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-28
---

# Phase 06.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.x |
| **Config file** | vitest.config.ts (project root) |
| **Quick run command** | `npx vitest run --passWithNoTests` |
| **Full suite command** | `npx vitest run` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run --passWithNoTests`
- **After every plan wave:** Run `npx vitest run`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| W0-01 | — | 0 | — | — | N/A | infra | `npx vitest run --passWithNoTests` | ✅ | ⬜ pending |
| * | * | * | REQ-011 | — | N/A | unit | `npx vitest run` | ✅ | ⬜ pending |
| * | * | * | REQ-012 | — | N/A | unit | `npx vitest run` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Test stubs for new `@aura/contract` types (`Maintenance.ts`, `EpistemicInspection.ts`) — verify TypeScript compilation
- [ ] Test stubs for EpistemicRuntime inspection methods — verify signatures match Rust
- [ ] Test stubs for MaintenanceService orchestration — verify Effect pipeline compiles
- [ ] Rust fixture bin (`aura-ts-fixtures`) — verify it builds and outputs JSON fixtures
- [ ] Cross-language fixture loader helper — verify it deserializes Rust fixture JSON into TS types

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Rust fixture output matches TS expected values | REQ-011, REQ-012 | Cross-language — requires Rust binary execution | Run `cargo run --bin aura-ts-fixtures` in Rust workspace, diff JSON output against TS test expectations |
| DefaultLayer wiring compiles after MaintenanceService addition | REQ-012 | Type-level verification | `npx tsc --noEmit` — verify no type errors in `@aura/core/src/DefaultLayer.ts` |
| Aura.ts optional runMaintenance() method | REQ-012 | Integration check | Manual inspection after implementation |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
