---
phase: 06.3-engine-algorithm-parity
plan: 11
type: execute
wave: 6
depends_on: [06.3-10]
files_modified:
  - AUDIT-DIFF.md
  - packages/core/src/MaintenanceService.ts
  - packages/core/src/MaintenanceService.test.ts
autonomous: false
requirements: [REQ-011, REQ-012]
user_setup: []

must_haves:
  truths:
    - "All 14 AUDIT-DIFF deviations marked as fixed with verification notes"
    - "End-to-end maintenance cycle runs without errors across all 4 engines"
    - "Full typecheck passes across all packages"
    - "All existing and new tests pass (bun run test)"
    - "MaintenanceService.runDiscoveryPhases correctly orchestrates all 4 engines with new signatures"
  artifacts:
    - path: AUDIT-DIFF.md
      provides: "Updated status of all deviations"
      contains: "Fixed"
    - path: packages/core/src/MaintenanceService.ts
      provides: "Final alignment with all engine signature changes"
      contains: "causalEngine.discover, policyEngine.discover"
  key_links:
    - from: packages/core/src/MaintenanceService.ts
      to: packages/causal/src/CausalEngine.ts
      via: "causalEngine.discover(beliefEngine, ...)"
    - from: packages/core/src/MaintenanceService.ts
      to: packages/policy/src/PolicyEngine.ts
      via: "policyEngine.discover(causalEngine, conceptEngine, beliefEngine, ...)"
---

<objective>
Run end-to-end verification: full typecheck across all packages, full test suite, identify and fix any cross-engine integration issues, update AUDIT-DIFF.md status, and produce final verification report.

Purpose: Close the loop — verify all 14 AUDIT-DIFF deviations are fixed, all 4 engines interoperate correctly, and the integration is ship-ready.
Output: Updated AUDIT-DIFF.md, passing full test suite, verification report.
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06.3-engine-algorithm-parity/06.3-CONTEXT.md
@.planning/phases/06.3-engine-algorithm-parity/06.3-RESEARCH.md
@AUDIT-DIFF.md

@packages/core/src/MaintenanceService.ts
@packages/core/src/MaintenanceService.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full typecheck + test suite + cross-engine integration fix</name>
  <files>
    Multiple files across all packages — discovered during fix cycles
  </files>
  <read_first>
    No specific file — run `bun run typecheck` and `bun run test` first, then fix issues
  </read_first>
  <action>
    Per D-26, D-27: Run full validation and fix cross-engine issues.

    1. Run `bun run typecheck` across all packages
       - If type errors: fix them:
         - Check packages/contract for export/import mismatches
         - Check CausalEngineImpl discover signature type compat (BeliefEngine.Interface)
         - Check PolicyEngineImpl discover signature type compat (3-engine input)
         - Check MaintenanceService runDiscoveryPhases for signature alignment
         - Check all .test.ts files for constructor/signature mismatches

    2. Run `bun run test` full suite (or `bun run test --filter "@aura/{belief,concept,causal,policy,core}"`)
       - If test failures: fix them:
         - Update test mocks/fixtures that construct CausalEngineState or PolicyEngineState with old field names
         - Update tests that depend on old hypothesis ID format (id12 → deterministic)
         - Update tests that depend on old belief IDs or hypothesis counts
         - Update tests that depend on concept-level CausalPattern structure
         - Update MaintenanceService test to pass 3 engines to policyEngine.discover
         - Ensure BeliefEngine tests pass with new SDR threshold (0.6 → 0.15) — test data may need adjustment

    3. Fix specifically:
       - MaintenanceService.test.ts: update mock engines to match new signatures
       - CausalEngine.test.ts: update test fixtures to use record-level edges (not concept co-occurrence)
       - PolicyEngine.test.ts: update test fixtures to pass 3 mocked engines
       - BeliefEngine.test.ts: verify all tests pass with new hypothesis structure
       - ConceptEngine.test.ts: verify CanonicalFeature and guard tests pass

    4. Unify import consistency:
       - Ensure all engine files import types from `@aura/contract` (not relative paths)
       - Ensure xxhash-wasm is imported consistently (lazy init pattern)
       - Verify SIMPLE/FULL markers are present on stubbed functions (per D10)
  </action>
  <acceptance_criteria>
    - `bun run typecheck` passes with zero errors across all packages
    - `bun run test` passes with zero failures across all packages
    - All test mocks updated to use new engine signatures
    - All CausalPattern references use new 20+ field structure
    - All PolicyHint references use new field names
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck 2>&1 && bun run test 2>&1</automated>
  </verify>
  <done>Full typecheck + test suite passes; all cross-engine integration issues fixed.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Update AUDIT-DIFF.md and produce verification report</name>
  <files>
    AUDIT-DIFF.md
  </files>
  <what-built>Updated AUDIT-DIFF.md with fix status for all 14 deviations + end-to-end verification report</what-built>
  <how-to-verify>
    1. Read the updated AUDIT-DIFF.md
    2. Verify all 14 deviation entries have `Status: Fixed` with brief verification note
    3. Verify the summary table at top shows 14/14 resolved
    4. Run the full test suite one final time to confirm green
    5. The verification report should be included in the 06.3-11-SUMMARY.md
  </how-to-verify>
  <resume-signal>Type "approved" or describe any remaining issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Cross-engine integration | Data flowing between all 4 engines |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-19 | Tampering | Full typecheck | mitigate | All violations surfaced by strict TS compilation |
| T-06.3-20 | Information Disclosure | AUDIT-DIFF.md | accept | Internal document; records fix status per deviation |
</threat_model>

<verification>
- `bun run typecheck` passes
- `bun run test` passes
- AUDIT-DIFF.md updated with all deviations fixed
</verification>

<success_criteria>
- All 14 AUDIT-DIFF deviations marked as fixed with verification notes
- Zero type errors across all packages
- Full test suite passes
- MaintenanceService orchestrates all 4 engines with correct signatures
</success_criteria>

<output>
`.planning/phases/06.3-engine-algorithm-parity/06.3-11-SUMMARY.md` when done
</output>
