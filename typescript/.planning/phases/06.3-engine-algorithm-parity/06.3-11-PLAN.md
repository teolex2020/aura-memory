---
phase: 06.3-engine-algorithm-parity
plan: 11
type: execute
wave: 6
depends_on: [06.3-10]
files_modified:
  - AUDIT-DIFF.md
  - packages/core/src/MaintenanceService.ts
  - packages/core/src/MaintenanceService.test.ts
autonomous: false
requirements: [REQ-011, REQ-012]
user_setup:
  - service: Rust fixture generator
    why: "End-to-end parity verification between Rust and TS engine outputs"
    bin_paths:
      - name: aura-ts-fixtures
        path: "D:/workspace/@yuyi919/external/AuraSDK/target/release/aura-ts-fixtures (or cargo run --bin aura-ts-fixtures)"
        purpose: "Generates Rust fixture brain state for known record inputs"
      - name: aura-ts-verify-recall
        path: "D:/workspace/@yuyi919/external/AuraSDK/target/release/aura-ts-verify-recall (or cargo run --bin aura-ts-verify-recall)"
        purpose: "Compares TS engine output against Rust-generated fixture"
    note: "If build artifacts are not available, run `cargo build --release --bin aura-ts-fixtures --bin aura-ts-verify-recall` from the Rust workspace root first"

must_haves:
  truths:
    - "All 14 AUDIT-DIFF deviations marked as fixed with verification notes (including per-engine type-level parity checks)"
    - "End-to-end maintenance cycle runs without errors across all 4 engines"
    - "Full typecheck passes across all packages"
    - "All existing and new tests pass (bun run test)"
    - "MaintenanceService.runDiscoveryPhases correctly orchestrates all 4 engines with new signatures"
    - "Rust fixture/verifier commands executed or attempted — concrete verification evidence recorded in SUMMARY"
    - "Per-engine parity tracking table populated: constants, thresholds, formulas verified for each engine"
  artifacts:
    - path: AUDIT-DIFF.md
      provides: "Updated status of all deviations with per-engine parity verification notes"
      contains: "Fixed"
    - path: packages/core/src/MaintenanceService.ts
      provides: "Final alignment with all engine signature changes"
      contains: "causalEngine.discover, policyEngine.discover"
  key_links:
    - from: packages/core/src/MaintenanceService.ts
      to: packages/causal/src/CausalEngine.ts
      via: "causalEngine.discover(beliefEngine, ...)"
    - from: packages/core/src/MaintenanceService.ts
      to: packages/policy/src/PolicyEngine.ts
      via: "policyEngine.discover(causalEngine, conceptEngine, beliefEngine, ...)"
---

<objective>
Run end-to-end verification: full typecheck across all packages, full test suite, Rust fixture verification (targeted + E2E), identify and fix any cross-engine integration issues, update AUDIT-DIFF.md status, and produce final verification report with concrete Rust-vs-TS evidence.

Purpose: Close the loop — verify all 14 AUDIT-DIFF deviations are fixed, all 4 engines interoperate correctly with Rust parity, and the integration is ship-ready.
Output: Updated AUDIT-DIFF.md, passing full test suite, verification report with concrete evidence.
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06.3-engine-algorithm-parity/06.3-CONTEXT.md
@.planning/phases/06.3-engine-algorithm-parity/06.3-RESEARCH.md
@AUDIT-DIFF.md

@packages/core/src/MaintenanceService.ts
@packages/core/src/MaintenanceService.test.ts

# Per-engine parity tracking — each engine plan should have verified:
# - Belief: constants/thresholds, contradiction split, deterministic IDs, subcluster dispatch (Plans 02-04)
# - Concept: word lists/equivalence dict extracted from Rust, surface ordering (Plans 05-06)
# - Causal: scoring formulas/weights from Rust, evidence gates, fingerprint inputs (Plans 07-08)
# - Policy: strength weights, recommendation templates, suppression provenance (Plans 09-10)
# This plan verifies E2E + captures any cross-engine discovery issues
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full typecheck + test suite + cross-engine integration fix + per-engine type-level parity verification</name>
  <files>
    Multiple files across all packages — discovered during fix cycles
  </files>
  <read_first>
    No specific file — run `bun run typecheck` and `bun run test` first, then fix issues
  </read_first>
  <action>
    Per D-26, D-27: Run full validation and fix cross-engine issues.

    PER-ENGINE TYPE-LEVEL PARITY VERIFICATION — Before running the full suite, verify each engine's contract-level alignment:

    1. **BeliefEngine type-level parity:**
       - Verify SDR_TANIMOTO_THRESHOLD == 0.15 (constant value)
       - Verify RECENCY_HALF_LIFE_SECS == 1209600 (14 days in seconds)
       - Verify splitByContradiction signature matches Rust split_by_contradiction
       - Verify deterministicHypothesisId produces deterministic output
       - Verify computeConsistency uses (n-1) divisor
       - Verify deprecate_belief halves confidence (not deletes)

    2. **ConceptEngine type-level parity:**
       - Verify stop word count and values match Rust (grep check or runtime assertion)
       - Verify equivalence dict entries match Rust (spot-check via grep)
       - Verify CanonicalFeature is default SimilarityMode
       - Verify surface threshold constants (MAX_SURFACED_PER_NAMESPACE=5, SURFACE_CANDIDATE_THRESHOLD=0.70)

    3. **CausalEngine type-level parity:**
       - Verify causal_strength formula weights match Rust (grep check)
       - Verify evidence gate thresholds (support>=2, repeated>=2, counterfactual<=0.50)
       - Verify corpus fingerprint input fields match Rust
       - Verify NearbySuccessors limit (16) and namespace cap (5000)

    4. **PolicyEngine type-level parity:**
       - Verify policyStrength formula weights match Rust
       - Verify recommendation templates match Rust string literals
       - Verify suppression thresholds and surface caps match Rust

    Record all findings in SUMMARY.md with grep commands confirming each value.

    **Integration test suite fixes:**
    1. Run `bun run typecheck` across all packages
       - If type errors: fix them:
         - Check packages/contract for export/import mismatches
         - Check CausalEngine discover signature type compat with BeliefEngine.Interface
         - Check PolicyEngine discover signature type compat with 3-engine Interface input
         - Check MaintenanceService runDiscoveryPhases for signature alignment
         - Check all .test.ts files for constructor/signature mismatches

    2. Run `bun run test` full suite
       - If test failures: fix them:
         - Update test mocks/fixtures that construct CausalEngineState or PolicyEngineState with old field names
         - Update tests that depend on old hypothesis ID format (id12 → deterministic)
         - Update tests that depend on old belief IDs or hypothesis counts
         - Update tests that depend on concept-level CausalPattern structure
         - Update MaintenanceService test to pass 3 engines to policyEngine.discover
         - Ensure BeliefEngine tests pass with new SDR threshold (0.6 → 0.15)

    3. Fix specifically:
       - MaintenanceService.test.ts: update mock engines to match new signatures
       - CausalEngine.test.ts: update test fixtures to use record-level edges (not concept co-occurrence)
       - PolicyEngine.test.ts: update test fixtures to pass 3 mocked engines using Interface types
       - BeliefEngine.test.ts: verify all tests pass with new hypothesis structure
       - ConceptEngine.test.ts: verify CanonicalFeature and guard tests pass

    4. Unify import consistency:
       - Ensure all engine files import types from `@aura/contract` (not relative paths)
       - Ensure xxhash-wasm is imported consistently (lazy init pattern)
       - Verify SIMPLE/FULL markers are present on stubbed functions (per D10)
  </action>
  <acceptance_criteria>
    - `bun run typecheck` passes with zero errors across all packages
    - `bun run test` passes with zero failures across all packages
    - All test mocks updated to use new engine signatures
    - All CausalPattern references use new 20+ field structure with provenance fields
    - All PolicyHint references use new field names and PolicyActionKind enum
    - Per-engine type-level parity checks recorded with grep evidence
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck 2>&1 && bun run test 2>&1</automated>
  </verify>
  <done>Full typecheck + test suite passes; per-engine type-level parity verified; all cross-engine integration issues fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Rust fixture verification (targeted + E2E)</name>
  <files>
    No new files — verification-only task
  </files>
  <read_first>
    @AUDIT-DIFF.md
  </read_first>
  <action>
    Per D-26, D-27: Run Rust fixture verification and E2E comparison.

    **Rust fixture commands (run from Rust workspace root or check if pre-built binaries exist):**
    These commands generate known brain state from Rust and compare against TS output.

    ```bash
    # Option A: If pre-built binaries exist
    #   ../../../target/release/aura-ts-fixtures --output-dir /tmp/rust-fixtures
    #   ../../../target/release/aura-ts-verify-recall --fixture-dir /tmp/rust-fixtures --ts-output-dir $TS_OUTPUT

    # Option B: Build-and-run
    #   cd ../../.. && cargo run --release --bin aura-ts-fixtures -- --output-dir /tmp/rust-fixtures
    #   cd ../../.. && cargo run --release --bin aura-ts-verify-recall -- --fixture-dir /tmp/rust-fixtures --ts-output-dir $TS_OUTPUT
    ```

    Steps:
    1. Check if Rust fixture binaries exist: `ls ../../../target/release/aura-ts-fixtures 2>/dev/null`
    2. If not, check if `cargo` is available: `which cargo 2>/dev/null` and the Rust project has the fixture bin defined
    3. If Rust binaries exist or can be built:
       a. Generate Rust fixtures: run aura-ts-fixtures to produce known brain state
       b. Run verify-recall: compare TS output vs Rust fixture
       c. Record any mismatches by pattern type (edge count, score, polarity, etc.)
    4. If Rust binaries are NOT available:
       a. Record in SUMMARY: "Rust fixture binaries not found at expected path — skipped E2E verification"
       b. Mark as pending: requires build of Rust workspace aura-ts-fixtures crate
       c. Fall back to hand-crafted test cases that replicate Rust test patterns

    **Verification evidence to capture in SUMMARY:**
    For each fixture test, record:
    - Fixture name / test case
    - Rust output value
    - TS output value
    - Match? (yes/no/approx)
    - Gap analysis (if any)

    **AUDIT-DIFF.md update:**
    For each of the 14 deviations, add a verification note:
    - Belief 1.x: "SRC_EXTRACTED — constants verified against Rust belief.rs at lines {X}; tests pass."
    - Concept 2.x: "SRC_EXTRACTED — word lists/equivalence dict copied from Rust concept.rs"
    - Causal 3.x: "SRC_EXTRACTED — scoring weights verified against Rust causal.rs score_pattern"
    - Policy 4.x: "SRC_EXTRACTED — formula weights and templates verified against Rust policy.rs"
  </action>
  <acceptance_criteria>
    - Rust fixture commands attempted with result recorded
    - If binaries available: comparison results recorded per pattern type
    - If binaries unavailable: documented as pending with fallback strategy noted
    - SUMMARY.md contains concrete verification evidence
  </acceptance_criteria>
  <verify>
    <automated>ls ../../../target/release/aura-ts-fixtures 2>/dev/null && echo "Binary exists" || echo "Binary not found — manual build needed"</automated>
  </verify>
  <done>Rust fixture verification attempted; results recorded in SUMMARY.md with concrete evidence.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Update AUDIT-DIFF.md and produce verification report</name>
  <files>
    AUDIT-DIFF.md
  </files>
  <what-built>
    Updated AUDIT-DIFF.md with fix status for all 14 deviations + per-engine type-level parity verification evidence + E2E verification report
  </what-built>
  <how-to-verify>
    1. Read the updated AUDIT-DIFF.md
    2. Verify all 14 deviation entries have `Status: Fixed` with verification note referencing the Rust source line or grep command used
    3. Verify the summary table at top shows 14/14 resolved
    4. Verify the SUMMARY.md (06.3-11-SUMMARY.md) includes:
       - Per-engine type-level parity tracking table (constants/thresholds/formulas verified)
       - Rust fixture verification results (or documented unavailability)
       - Concrete Rust-vs-TS evidence (grep commands, constant values, formula weights)
    5. Run the full test suite one final time to confirm green
  </how-to-verify>
  <resume-signal>Type "approved" or describe any remaining issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Cross-engine integration | Data flowing between all 4 engines |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-19 | Tampering | Full typecheck | mitigate | All violations surfaced by strict TS compilation workspace-wide |
| T-06.3-20 | Information Disclosure | AUDIT-DIFF.md | accept | Internal document; records fix status per deviation with Rust parity evidence |
</threat_model>

<verification>
- `bun run typecheck` passes across all packages
- `bun run test` passes across all packages
- AUDIT-DIFF.md updated with all deviations fixed and Rust parity evidence
- SUMMARY.md includes per-engine parity tracking table + Rust fixture verification results
</verification>

<success_criteria>
- All 14 AUDIT-DIFF deviations marked as fixed with Rust-source verification notes (SRC_EXTRACTED markers)
- Per-engine type-level parity tracking table in SUMMARY with grep-evidenced constants/thresholds/formulas
- Zero type errors across all packages
- Full test suite passes
- MaintenanceService orchestrates all 4 engines with correct signatures
- Rust fixture verification attempted with concrete results recorded
- SUMMARY.md contains concrete Rust-vs-TS verification evidence (not just "tests passed")
</success_criteria>

<output>
`.planning/phases/06.3-engine-algorithm-parity/06.3-11-SUMMARY.md` when done
</output>
