---
phase: 06.3-engine-algorithm-parity
plan: 02
type: tdd
wave: 2
depends_on: [06.3-01]
files_modified:
  - packages/belief/src/BeliefEngine.ts
  - packages/belief/src/BeliefEngine.test.ts
  - packages/belief/src/index.ts
autonomous: true
requirements: [REQ-011]
user_setup: []

must_haves:
  truths:
    - "SDR Tanimoto threshold changed from 0.6 to 0.15"
    - "Recency half-life changed from 7 days to 14 days"
    - "Content filter uses byte length (< 10 bytes) instead of token estimate"
    - "Hypothesis IDs are deterministic xxh3_64(belief_id + '\\0' + sorted_record_ids)"
    - "split_by_contradiction divides records into supporting/opposing (max 2 hypotheses per belief)"
    - "Hypothesis consistency uses sample variance (n-1 divisor) not population variance"
    - "Belief update is incremental (preserves key_index, record_index, reuses existing beliefs)"
    - "BeliefReport reports 7 fields including churn_rate"
    - "deprecate_belief halves confidence, sets state Unresolved (does not delete)"
  artifacts:
    - path: packages/belief/src/BeliefEngine.ts
      provides: "Rewritten P0 functions: split_by_contradiction, deterministicHypothesisId, incremental update_with_sdr"
      contains: "SDR_TANIMOTO_THRESHOLD = 0.15, RECENCY_HALF_LIFE_SECS = 14"
    - path: packages/belief/src/BeliefEngine.test.ts
      provides: "Tests for contradiction split, deterministic IDs, incremental update"
      contains: "test('deterministic hypothesis id',"
  key_links:
    - from: packages/belief/src/BeliefEngine.ts
      to: packages/contract/src/belief/BeliefTypes.ts
      via: "import BeliefReport, Belief, BeliefEngineState"
      pattern: "import.*BeliefTypes"
    - from: packages/belief/src/BeliefEngine.ts
      to: packages/contract/src/Causal.ts,Policy.ts
      via: "No direct dep in P0 — but must use xxhash-wasm for deterministic IDs"
---

<objective>
Align BeliefEngine P0 with Rust: contradiction-based hypothesis split, SDR threshold 0.15, recency 14 days, content filter byte-length, sample variance consistency, deterministic hypothesis IDs, incremental update, soft deprecation.

Purpose: Fix 4 systematic P0 deviations (AUDIT-DIFF 1.1, 1.4, 1.5, 1.6) — the foundational architectural changes to belief formation.
Output: Rewritten BeliefEngine.ts with Rust-aligned P0 logic, passing test suite.
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/workflows/execute-plan.md
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3-engine-algorithm-parity/06.3-CONTEXT.md
@.planning/phases/06.3-engine-algorithm-parity/06.3-RESEARCH.md
@AUDIT-DIFF.md
@EXPLAIN.md

# Rust reference — read before editing
@../src/belief.rs

# Current TS implementation
@packages/belief/src/BeliefEngine.ts
@packages/belief/src/BeliefEngine.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Threshold + constant changes + split_by_contradiction + deterministic IDs + sample variance + content filter</name>
  <files>
    packages/belief/src/BeliefEngine.ts
    packages/belief/src/BeliefEngine.test.ts
  </files>
  <read_first>
    packages/belief/src/BeliefEngine.ts
    packages/belief/src/BeliefEngine.test.ts
    @EXPLAIN.md
    @AUDIT-DIFF.md
  </read_first>
  <behavior>
    Test 1: split_by_contradiction splits records into supporting/opposing based on semantic_type and conflict_mass/support_mass comparison
    Test 2: deterministicHypothesisId produces same ID for same belief_id + sorted record_ids across calls
    Test 3: deterministicHypothesisId produces different IDs for different record sets
    Test 4: computeConsistency uses sample variance (n-1) — returns 1.0 for length 1, expected value for length 2+
    Test 5: Content filter skips records with content.length < 10 (all whitespace trimmed length)
    Test 6: SDR_TANIMOTO_THRESHOLD exported constant = 0.15
    Test 7: RECENCY_HALF_LIFE_SECS exported constant = 14 * 24 * 3600
  </behavior>
  <action>
    Per D-02, D-03, D-06, D-07, D-09: Implement P0 changes as pure functions in BeliefEngine.ts.

    Changes to constants (top of file):
    - SDR_TANIMOTO_THRESHOLD: 0.6 → 0.15
    - RECENCY_HALF_LIFE_SECS: 7 * 24 * 3600 → 14 * 24 * 3600
    - Add SDR_TAG_GUARD_THRESHOLD = 0.10, TAG_SDR_FINGERPRINT_THRESHOLD = 0.08 (used in later plans)

    Content filter change:
    - Remove estimateTokens() import/usage
    - Replace `if (estimateTokens(rec.content) < MIN_CONTENT_TOKENS) continue` with `if (rec.content.length < 10) continue` per Rust's `content.len() < 10`
    - Remove MIN_CONTENT_TOKENS constant (5), remove estimateTokens function

    computeConsistency change:
    - Change variance divisor from `/ confidences.length` to `/ (confidences.length - 1)` (sample variance)
    - Guard: if confidences.length <= 1, return 1.0 (already handled)

    New function: splitByContradiction(records: ReadonlyArray<AuraRecord>): [AuraRecord[], AuraRecord[]]
    - Mirrors Rust split_by_contradiction at belief.rs:1238-1251
    - Iterates records; if rec.semantic_type === "contradiction" OR rec.conflict_mass > rec.support_mass, pushes to opposing; else pushes to supporting
    - Returns [supporting, opposing]

    New function: deterministicHypothesisId(hasher: { h64: (input: string) => bigint }, beliefId: string, records: ReadonlyArray<AuraRecord>): string
    - Mirrors Rust Hypothesis::deterministic_id at belief.rs:179
    - Sorts record IDs alphabetically
    - Joins with '\0' separator: beliefId + "\0" + sorted_ids
    - Runs xxh3_64 via hasher.h64(), masks to 64 bits
    - Returns hex string padded to 12 chars (padStart(12, '0'))

    Update hypothesisFromRecords:
    - Replace `id: id12()` with `id: deterministicHypothesisId(...)` — requires hasher instance
    - Keep existing logic for confidence, support_mass, conflict_mass, recency, consistency calculations
    - Import xxhash-wasm at module level (reuse existing pattern from CausalEngine.ts)
    - Use lazy initialization of xxhash hasher (async, stored in module-level variable)

    Write tests in BeliefEngine.test.ts covering all behaviors above before implementation (TDD RED phase).
  </action>
  <acceptance_criteria>
    - splitByContradiction splits records correctly: contradiction semantic_type → opposing, conflict_mass > support_mass → opposing
    - deterministicHypothesisId returns same 12-char hex for same inputs across calls
    - computeConsistency uses divisor (n-1): for length=2 with values [0.5, 1.0], variance = ((0.5-0.75)^2 + (1.0-0.75)^2) / 1
    - Content filter: record with content "hi" (len < 10) is skipped
    - SDR_TANIMOTO_THRESHOLD constant = 0.15
    - "bun run test --filter '@aura/belief'" passes all tests
  </acceptance_criteria>
  <verify>
    <automated>cd packages/belief && bun run typecheck 2>&1 && bun run test --reporter verbose 2>&1</automated>
  </verify>
  <done>All P0 primitive functions implemented with tests; test suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Incremental update_with_sdr + soft deprecate_belief</name>
  <files>
    packages/belief/src/BeliefEngine.ts
    packages/belief/src/BeliefEngine.test.ts
  </files>
  <read_first>
    packages/belief/src/BeliefEngine.ts
    packages/belief/src/BeliefEngine.test.ts
    @../src/belief.rs (lines 976-1232 for update_with_sdr, lines 1264-1285 for deprecate_belief)
  </read_first>
  <behavior>
    Test 1: update_with_sdr preserves key_index across calls (incremental)
    Test 2: update_with_sdr reuses existing belief when same coarse key exists
    Test 3: update_with_sdr prunes stale beliefs (records no longer present)
    Test 4: update_with_sdr returns BeliefReport with all 7 fields populated
    Test 5: deprecate_belief halves confidence, sets state to Unresolved, does NOT delete belief
    Test 6: record_index maps record_id → hypothesis_id (not belief_id directly)
  </behavior>
  <action>
    Per D-10 and D-03: Rewrite update_with_sdr as incremental update with key_index/record_index.

    Add to BeliefEngineState usage:
    - key_index: Map<string, string> — maps coarse key → belief_id
    - record_index: Map<string, string> — maps record_id → hypothesis_id

    BeliefEngineImpl changes:
    - Add private fields: keyIndex: Map<string, string>, recordIndex: Map<string, string>
    - Initialize in constructor/before each cycle
    - stats() returns state with key_index and record_index populated

    update_with_sdr rewrite (new algorithm):
    1. Build new key_index from current records (matching coarse keys)
    2. For each coarse key:
       a. Look up existing belief in key_index → if exists and key matches → reuse belief_id
       b. If new key → create new belief
       c. Within coarse group: use splitByContradiction instead of unionFindClusters
       d. For supporting records → one hypothesis (or two if both sides have records)
       e. Use deterministicHypothesisId for hypothesis ID
    3. For each existing belief not in new key_index → deprecate (halve confidence, set Unresolved)
    4. Build record_index: for each hypothesis, map record_id → hypothesis_id
    5. Report includes: beliefs_created, beliefs_pruned, revisions, resolved, unresolved, total_beliefs, total_hypotheses, churn_rate

    Deprecate belief change:
    - Rewrite deprecate_belief: halve the belief's confidence (not delete), set state to Unresolved, set winner_id to null
    - Keep hypotheses but mark them as deprecated (add deprecated flag or reduce score)
    - Do NOT remove the belief from state
    - Rust behavior: confidence /= 2.0, state = Unresolved, winner = None

    Write tests for all behaviors first (TDD RED phase), then implement.
  </action>
  <acceptance_criteria>
    - Key_index persists across update_with_sdr calls: same records → same belief IDs
    - Adding a new record to a known coarse group → existing belief gets new hypothesis (revision)
    - Removing a record → existing belief stays but its report reflects pruning
    - deprecate_belief halves confidence: belief with confidence 0.8 becomes 0.4
    - deprecate_belief sets state to "Unresolved" and winner_id to null
    - BeliefReport has beliefs_created, beliefs_pruned, revisions, resolved, unresolved, total_beliefs, total_hypotheses, churn_rate
    - "bun run test --filter '@aura/belief'" passes all tests
  </acceptance_criteria>
  <verify>
    <automated>cd packages/belief && bun run typecheck 2>&1 && bun run test --reporter verbose 2>&1</automated>
  </verify>
  <done>Incremental update_with_sdr implemented with key_index/record_index; soft deprecate_belief implemented; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| BeliefEngine state | Mutable state updated each maintenance cycle |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-02 | Tampering | deterministicHypothesisId | mitigate | xxh3-64 hash provides 2^64 space; collision probability negligible for typical record sets |
| T-06.3-03 | Tampering | Incremental update | mitigate | Key_index ensures consistent belief reuse; stale belief pruning via confidence halving not deletion |
| T-06.3-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
- `bun run test --filter "@aura/belief"` passes all tests
- All constants verified: SDR_TANIMOTO_THRESHOLD=0.15, RECENCY_HALF_LIFE_SECS=1209600
</verification>

<success_criteria>
- P0 all constants and primitives aligned with Rust
- split_by_contradiction replaces unionFindClusters for hypothesis generation
- update_with_sdr is incremental (key_index/record_index maintained)
- deprecate_belief soft-deprecates (halve confidence, Unresolved) not hard-deletes
- All tests pass
</success_criteria>

<output>
`.planning/phases/06.3-engine-algorithm-parity/06.3-02-SUMMARY.md` when done
</output>
