---
phase: 06-maintenance-pipeline-completion
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/contract/src/causal/CausalTypes.ts (NEW)
  - packages/contract/src/policy/PolicyTypes.ts (NEW)
  - packages/contract/src/Causal.ts (UPDATE - typed interfaces)
  - packages/contract/src/Policy.ts (UPDATE - typed interfaces)
  - packages/contract/src/EpistemicRuntime.ts (UPDATE - add maintain signature)
  - packages/contract/src/index.ts (UPDATE - add new type exports)
  - packages/storage/src/CausalStoreFile.ts (UPDATE - typed engine state)
  - packages/storage/src/PolicyStoreFile.ts (UPDATE - typed engine state)
  - packages/causal/src/CausalStore.ts (UPDATE - typed engine state)
  - packages/policy/src/PolicyStore.ts (UPDATE - typed engine state)
autonomous: true
requirements:
  - REQ-012

must_haves:
  truths:
    - CausalTypes.ts defines CausalEngineState, CausalPattern, CausalReport, CausalState enum
    - PolicyTypes.ts defines PolicyEngineState, PolicyHint, PolicyReport, PolicyState enum
    - Causal.ts exports CausalEngineImpl with typed discover(invalidate/retract/stats)
    - Policy.ts exports PolicyEngineImpl with typed discover/retract_hint/stats
    - CausalStoreImpl uses CausalEngineState (not unknown)
    - PolicyStoreImpl uses PolicyEngineState (not unknown)
    - CausalStoreFile uses CausalEngineState (not unknown)
    - PolicyStoreFile uses PolicyEngineState (not unknown)
    - EpistemicRuntimeImpl has maintain() signature
    - Contract index.ts re-exports causal/CausalTypes and policy/PolicyTypes
  artifacts:
    - path: packages/contract/src/causal/CausalTypes.ts
      provides: Causal type definitions matching ConceptTypes.ts pattern
      min_lines: 50
    - path: packages/contract/src/policy/PolicyTypes.ts
      provides: Policy type definitions matching ConceptTypes.ts pattern
      min_lines: 40
    - path: packages/contract/src/Causal.ts
      provides: CausalEngineImpl with typed discover, invalidate_pattern, retract_pattern, stats
      exports: ["CausalEngineImpl", "CausalEngine", "CausalStoreImpl", "CausalStore"]
    - path: packages/contract/src/Policy.ts
      provides: PolicyEngineImpl with typed discover, retract_hint, stats
      exports: ["PolicyEngineImpl", "PolicyEngine", "PolicyStoreImpl", "PolicyStore"]
    - path: packages/contract/src/EpistemicRuntime.ts
      provides: EpistemicRuntimeImpl with maintain()
      exports: ["EpistemicRuntimeImpl", "EpistemicRuntime"]
  key_links:
    - from: packages/contract/src/Causal.ts
      to: packages/contract/src/causal/CausalTypes.ts
      via: import of CausalEngineState, CausalReport
      pattern: "import.*CausalEngineState|import.*CausalReport"
    - from: packages/contract/src/Policy.ts
      to: packages/contract/src/policy/PolicyTypes.ts
      via: import of PolicyEngineState, PolicyReport
      pattern: "import.*PolicyEngineState|import.*PolicyReport"
    - from: packages/contract/src/EpistemicRuntime.ts
      to: packages/contract/src/causal/CausalTypes.ts
      via: import of CausalReport
      pattern: "import.*CausalReport"
    - from: packages/contract/src/EpistemicRuntime.ts
      to: packages/contract/src/policy/PolicyTypes.ts
      via: import of PolicyReport
      pattern: "import.*PolicyReport"
---

<objective>
Create typed contract definitions for Causal and Policy engine domains, following the existing `belief/BeliefTypes.ts` and `concept/ConceptTypes.ts` patterns. Update the Causal, Policy, and EpistemicRuntime service interfaces to use proper typed signatures instead of `unknown`/`UnimplementedError`. Update store files to use typed engine states.

Purpose: Replace generic `unknown` types in engine interfaces with concrete domain types, enabling type-safe engine implementations in subsequent plans. The store files in storage/, causal/, and policy/ packages must also use the new typed states.

Output: 2 new type definition files, 5 updated contract/service interface files, 2 updated store wrapper files, 2 updated store file files.
</objective>

<execution_context>
@/workspace/@yuyi919/external/AuraSDK/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/@yuyi919/external/AuraSDK/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@packages/contract/src/belief/BeliefTypes.ts
@packages/contract/src/concept/ConceptTypes.ts
@packages/contract/src/Causal.ts
@packages/contract/src/Policy.ts
@packages/contract/src/EpistemicRuntime.ts
@packages/contract/src/Concept.ts
@packages/contract/src/Belief.ts
@packages/contract/src/index.ts
@packages/contract/src/Recall.ts
@packages/storage/src/CausalStoreFile.ts
@packages/storage/src/PolicyStoreFile.ts
@packages/causal/src/CausalStore.ts
@packages/policy/src/PolicyStore.ts
@packages/causal/src/CausalEngine.ts
@packages/policy/src/PolicyEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create causal/CausalTypes.ts and policy/PolicyTypes.ts in contract</name>
  <files>
    <read_first>
      packages/contract/src/concept/ConceptTypes.ts
      packages/contract/src/belief/BeliefTypes.ts
      packages/contract/src/Causal.ts
      packages/contract/src/Policy.ts
      packages/causal/src/CausalEngine.ts
      packages/policy/src/PolicyEngine.ts
    </read_first>
    <create>packages/contract/src/causal/CausalTypes.ts</create>
    <create>packages/contract/src/policy/PolicyTypes.ts</create>
  </files>
  <action>
    Create `packages/contract/src/causal/CausalTypes.ts` following the exact pattern of `concept/ConceptTypes.ts`.

    The file MUST export:
    - Enum `CausalDiscoveryMode` with values `Standard` and `Strict`
    - Type `CausalPatternId = string`
    - Enum `CausalState` with values `Candidate`, `Stable`, `Rejected`, `Invalidated` (reuse the 4 values from the existing CausalEngine.ts local enum)
    - Type `CausalPattern` — a readonly object with fields: `id: CausalPatternId`, `antecedent_concept_ids: ReadonlyArray<string>`, `consequent_concept_ids: ReadonlyArray<string>`, `support: number`, `confidence: number`, `lift: number`, `state: CausalState`, `last_updated: number`. Include JSDoc comments for each field (Chinese+English), matching the style of ConceptCandidate in ConceptTypes.ts.
    - Type `CausalEngineState` — a readonly object with `version: 1`, `patterns: Readonly<Record<string, CausalPattern>>`, `discovery_mode: CausalDiscoveryMode`. JSDoc saying "Snapshot of the full causal engine state for persistence/downstream layers." Include Chinese translation.
    - Type `CausalReport` — a readonly object with `patterns_found: number`, `patterns_active: number`, `patterns_invalidated: number`, `avg_confidence: number`, `avg_lift: number`. JSDoc saying "Per-cycle report from CausalEngine.discover for trace/observability." Include Chinese translation. Use `assert.strictEqual`-friendly numeric fields (no Option, no undefined).

    Create `packages/contract/src/policy/PolicyTypes.ts` following the exact pattern of `concept/ConceptTypes.ts`.

    The file MUST export:
    - Type `PolicyHintId = string`
    - Enum `PolicyState` with values `Candidate`, `Stable`, `Suppressed`, `Rejected` (reuse the 4 values from the existing PolicyEngine.ts local enum)
    - Type `PolicyHint` — a readonly object with fields: `id: PolicyHintId`, `pattern_id: string | null`, `condition: string`, `action: string`, `priority: number`, `confidence: number`, `state: PolicyState`, `last_updated: number`. Include JSDoc comments (Chinese+English).
    - Type `PolicyEngineState` — a readonly object with `version: 1`, `hints: Readonly<Record<string, PolicyHint>>`, `metadata: Readonly<Record<string, unknown>>`. JSDoc (Chinese+English).
    - Type `PolicyReport` — a readonly object with `hints_found: number`, `hints_active: number`, `hints_suppressed: number`, `avg_confidence: number`. JSDoc (Chinese+English).
  </action>
  <verify>
    <automated>grep -c "export enum CausalState" packages/contract/src/causal/CausalTypes.ts && grep -c "export type CausalEngineState" packages/contract/src/causal/CausalTypes.ts && grep -c "export type CausalReport" packages/contract/src/causal/CausalTypes.ts && grep -c "export enum PolicyState" packages/contract/src/policy/PolicyTypes.ts && grep -c "export type PolicyEngineState" packages/contract/src/policy/PolicyTypes.ts && grep -c "export type PolicyReport" packages/contract/src/policy/PolicyTypes.ts</automated>
  </verify>
  <done>
    - CausalTypes.ts exists with all required exports (CausalState, CausalPattern, CausalEngineState, CausalReport, CausalDiscoveryMode)
    - PolicyTypes.ts exists with all required exports (PolicyState, PolicyHint, PolicyEngineState, PolicyReport)
    - Both files follow ConceptTypes.ts pattern (JSDoc, readonly fields, enums)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Causal.ts and Policy.ts contract interfaces to use typed signatures</name>
  <files>
    <read_first>
      packages/contract/src/Causal.ts
      packages/contract/src/Policy.ts
      packages/contract/src/Concept.ts (reference pattern)
      packages/contract/src/Belief.ts (reference pattern)
      packages/contract/src/causal/CausalTypes.ts
      packages/contract/src/policy/PolicyTypes.ts
    </read_first>
    <update>packages/contract/src/Causal.ts</update>
    <update>packages/contract/src/Policy.ts</update>
  </files>
  <action>
    Update `packages/contract/src/Causal.ts`:
    - Add imports: `CausalEngineState, CausalReport` from `./causal/CausalTypes`, `ConceptEngineState` from `./concept/ConceptTypes`, `SdrLookup` from `./sdr/Sdr`, `Record as AuraRecord` from `./record/Record`, `EpistemicTrace` from `./EpistemicTrace`
    - Change `CausalEngineImpl` type:
      - `discover: (concept_state: ConceptEngineState, records: ReadonlyMap<string, AuraRecord>, sdr_lookup: SdrLookup) => Effect.Effect<CausalReport, never, EpistemicTrace>`
      - `invalidate_pattern: (id: string) => Effect.Effect<void>`
      - `retract_pattern: (id: string) => Effect.Effect<void>`
      - `stats: () => Effect.Effect<CausalEngineState>`
    - Remove `UnimplementedError` from imports if no longer used in error types
    - Change `CausalStoreImpl` type:
      - `load: () => Effect.Effect<CausalEngineState, FileReadError | JsonParseError, FileRead>`
      - `save: (engine: CausalEngineState) => Effect.Effect<void, FileWriteError, FileWrite>`
    - Keep `CausalStore` and `CausalEngine` Tag classes unchanged

    Update `packages/contract/src/Policy.ts`:
    - Add imports: `PolicyEngineState, PolicyReport` from `./policy/PolicyTypes`, `CausalEngineState` from `./causal/CausalTypes`, `Record as AuraRecord` from `./record/Record`, `EpistemicTrace` from `./EpistemicTrace`
    - Change `PolicyEngineImpl` type:
      - `discover: (causal_state: CausalEngineState, records: ReadonlyMap<string, AuraRecord>) => Effect.Effect<PolicyReport, never, EpistemicTrace>`
      - `retract_hint: (id: string) => Effect.Effect<void>`
      - `stats: () => Effect.Effect<PolicyEngineState>`
    - Remove `UnimplementedError` from imports
    - Change `PolicyStoreImpl` type:
      - `load: () => Effect.Effect<PolicyEngineState, FileReadError | JsonParseError, FileRead>`
      - `save: (engine: PolicyEngineState) => Effect.Effect<void, FileWriteError, FileWrite>`
    - Keep `PolicyStore` and `PolicyEngine` Tag classes unchanged
  </action>
  <verify>
    <automated>cd packages/contract && grep -c "concept_state: ConceptEngineState" src/Causal.ts && grep -c "records: ReadonlyMap<string, AuraRecord>" src/Causal.ts && grep -c "Effect.Effect<CausalReport" src/Causal.ts && grep -c "causal_state: CausalEngineState" src/Policy.ts && grep -c "Effect.Effect<PolicyReport" src/Policy.ts && grep -c "Effect.Effect<CausalEngineState>" src/Causal.ts && grep -c "Effect.Effect<PolicyEngineState>" src/Policy.ts</automated>
  </verify>
  <done>
    - CausalEngineImpl.discover takes (ConceptEngineState, ReadonlyMap<string, AuraRecord>, SdrLookup) and returns Effect.Effect<CausalReport, never, EpistemicTrace>
    - CausalStoreImpl uses CausalEngineState consistently
    - PolicyEngineImpl.discover takes (CausalEngineState, ReadonlyMap<string, AuraRecord>) and returns Effect.Effect<PolicyReport, never, EpistemicTrace>
    - PolicyStoreImpl uses PolicyEngineState consistently
  </done>
</task>

<task type="auto">
  <name>Task 3: Update store files to use typed engine states + contract index.ts exports + EpistemicRuntime.ts maintain signature</name>
  <files>
    <read_first>
      packages/storage/src/CausalStoreFile.ts
      packages/storage/src/PolicyStoreFile.ts
      packages/causal/src/CausalStore.ts
      packages/policy/src/PolicyStore.ts
      packages/contract/src/index.ts
      packages/contract/src/EpistemicRuntime.ts
      packages/contract/src/causal/CausalTypes.ts
      packages/contract/src/policy/PolicyTypes.ts
      packages/contract/src/concept/ConceptTypes.ts
    </read_first>
    <update>packages/storage/src/CausalStoreFile.ts</update>
    <update>packages/storage/src/PolicyStoreFile.ts</update>
    <update>packages/causal/src/CausalStore.ts</update>
    <update>packages/policy/src/PolicyStore.ts</update>
    <update>packages/contract/src/index.ts</update>
    <update>packages/contract/src/EpistemicRuntime.ts</update>
  </files>
  <action>
    Update each store file to use typed engine states from @aura/contract:

    **`packages/storage/src/CausalStoreFile.ts`:**
    - Import `CausalEngineState` from `@aura/contract`
    - Import `CausalReport` from `@aura/contract` 
    - Change `empty_engine()` return type to `CausalEngineState` and return `{ version: 1 as const, patterns: {}, discovery_mode: CausalDiscoveryMode.Standard }` — import `CausalDiscoveryMode` if available, otherwise inline as `"Standard"`
    - Change `save(_engine: unknown)` to `save(engine: CausalEngineState)`
    - Change `load()` return type to `Effect.Effect<CausalEngineState, FileReadError | JsonParseError, FileRead>`

    **`packages/storage/src/PolicyStoreFile.ts`:**
    - Import `PolicyEngineState` from `@aura/contract`
    - Change `empty_engine()` return type to `PolicyEngineState` and return `{ version: 1 as const, hints: {}, metadata: {} }`
    - Change `save(_engine: unknown)` to `save(engine: PolicyEngineState)`
    - Change `load()` return type to `Effect.Effect<PolicyEngineState, FileReadError | JsonParseError, FileRead>`

    **`packages/causal/src/CausalStore.ts`:**
    - Import `CausalEngineState` from `@aura/contract`
    - Change `save(engine: unknown)` to `save(engine: CausalEngineState)`

    **`packages/policy/src/PolicyStore.ts`:**
    - Import `PolicyEngineState` from `@aura/contract`
    - Change `save(engine: unknown)` to `save(engine: PolicyEngineState)`

    **`packages/contract/src/index.ts`:**
    - Add `export * from "./causal/CausalTypes"` after the existing `export * from "./concept/ConceptTypes"` line
    - Add `export * from "./policy/PolicyTypes"` after the new causal export

    **`packages/contract/src/EpistemicRuntime.ts`:**
    - Add imports: `BeliefReport` from `./belief/BeliefTypes`, `ConceptReport` from `./concept/ConceptTypes`, `CausalReport` from `./causal/CausalTypes`, `PolicyReport` from `./policy/PolicyTypes`, `SdrLookup` from `./sdr/Sdr`, `Record as AuraRecord` from `./record/Record`, `EpistemicTrace` from `./EpistemicTrace`, `Effect` from `effect`
    - Define new type `EpistemicReport = { readonly belief: BeliefReport; readonly concept: ConceptReport; readonly causal: CausalReport; readonly policy: PolicyReport }`
    - Add to `EpistemicRuntimeImpl`: `maintain: (records: ReadonlyMap<string, AuraRecord>, sdr_lookup: SdrLookup) => Effect.Effect<EpistemicReport, never, EpistemicTrace>`
    - Update existing getters to return proper types:
      - `get_beliefs: () => Effect.Effect<BeliefEngineState>` (import from ./belief/BeliefTypes)
      - `get_concepts: () => Effect.Effect<ConceptEngineState>` (import from ./concept/ConceptTypes)  
      - `get_causal_patterns: () => Effect.Effect<CausalEngineState>` (import from ./causal/CausalTypes)
      - `get_policy_hints: () => Effect.Effect<PolicyEngineState>` (import from ./policy/PolicyTypes)
      - Keep `get_surfaced_concepts` and `get_surfaced_policy_hints` as `Effect.Effect<unknown>` — their signatures are not part of Phase 6
    - Remove `UnimplementedError` import (no longer used)
  </action>
  <verify>
    <automated>bun run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>
    - CausalStoreFile.save accepts CausalEngineState, empty_engine returns typed state
    - PolicyStoreFile.save accepts PolicyEngineState, empty_engine returns typed state
    - CausalStore.save accepts CausalEngineState
    - PolicyStore.save accepts PolicyEngineState
    - contract/index.ts exports causal/CausalTypes and policy/PolicyTypes
    - EpistemicRuntimeImpl has maintain() signature with EpistemicReport
    - `bun run typecheck` passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract/engine interfaces | No new trust boundary — types exist at compile time only, no runtime data crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | N/A | accept | Phase 6 is a pure types/contract layer. No runtime data, no new attack surface. |
</threat_model>

<verification>
1. `bun run typecheck` passes across all packages
2. `grep` confirms all typed exports exist
</verification>

<success_criteria>
- All contract type files created and exported from index.ts
- Causal.ts and Policy.ts use fully typed interfaces (CausalEngineState, CausalReport, PolicyEngineState, PolicyReport)
- EpistemicRuntime.ts has maintain() signature with EpistemicReport
- Store files use typed engine states
- `bun run typecheck` passes
</success_criteria>

<output>
Create `.planning/phases/06-maintenance-pipeline-completion/06-01-SUMMARY.md` when done
</output>
