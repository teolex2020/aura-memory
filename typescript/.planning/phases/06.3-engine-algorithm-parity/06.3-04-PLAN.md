---
phase: 06.3-engine-algorithm-parity
plan: 04
type: tdd
wave: 2
depends_on: [06.3-03]
files_modified:
  - packages/belief/src/BeliefEngine.ts
  - packages/belief/src/BeliefEngine.test.ts
autonomous: true
requirements: [REQ-011]
user_setup: []

must_haves:
  truths:
    - "BridgeKey normalize function has default mapping table (ui/frontend→frontend, auth/authentication→authentication, deploy/release→release, db/database→database, etc.)"
    - "Normalize function supports external configuration override"
    - "TagFamilyAdaptive does 2-pass clustering: strict then relaxed"
    - "TagFamilyBackoff falls back to broad bucket when all records in family are singletons"
    - "TagFamilyDenseBackoff detects stable secondary tags and re-groups"
    - "apply_layer_feedback accepts CausalEngineImpl + PolicyEngineImpl, processes both signal sources"
    - "apply_layer_feedback uses bounded clamping: confidence delta [-0.18, +0.08], volatility delta [-0.06, +0.20]"
    - "apply_layer_feedback returns BeliefFeedbackReport with 6 fields"
    - "dense_corridor_stable_tags function extracts stable secondary tags"
    - "dense_backoff_group_key function builds dense backoff group key"
  artifacts:
    - path: packages/belief/src/BeliefEngine.ts
      provides: "BridgeKey normalize, TagFamily backoff strategies, apply_layer_feedback"
      contains: "normalizeBridgeTag, denseCorridorStableTags, denseBackoffGroupKey, apply_layer_feedback"
  key_links:
    - from: packages/belief/src/BeliefEngine.ts
      to: @../src/belief.rs (bridge normalize: 526-570, backoff: 570-620, layer feedback: 1287-1350)
---

<objective>
Implement BeliefEngine P2: BridgeKey normalize table, TagFamilyAdaptive/Backoff/DenseBackoff strategies, apply_layer_feedback rewrite with bounded clamping and BeliefFeedbackReport.

Purpose: Close AUDIT-DIFF 1.7, 1.8 — bridge key normalization, 3 backoff strategies, and layer feedback alignment.
Output: Complete BeliefEngine.ts with all Rust P0-P1-P2 algorithms, passing test suite.
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06.3-engine-algorithm-parity/06.3-CONTEXT.md
@.planning/phases/06.3-engine-algorithm-parity/06.3-RESEARCH.md
@AUDIT-DIFF.md

# Rust reference
@../src/belief.rs

# Current belief engine (after P0 and P1 changes)
@packages/belief/src/BeliefEngine.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BridgeKey normalize + TagFamily backoff strategies</name>
  <files>
    packages/belief/src/BeliefEngine.ts
    packages/belief/src/BeliefEngine.test.ts
  </files>
  <read_first>
    @../src/belief.rs (lines 526-620 for bridge normalize and dense corridor, lines 570-620 for backoff)
    packages/belief/src/BeliefEngine.ts
  </read_first>
  <behavior>
    Test 1: normalizeBridgeTag maps "ui/frontend" → "frontend" per default bridge table
    Test 2: normalizeBridgeTag maps "auth/authentication" → "authentication" per default bridge table
    Test 3: normalizeBridgeTag returns the tag unchanged if no bridge mapping exists
    Test 4: normalizeBridgeTag supports external config override (priority over defaults)
    Test 5: TagFamilyAdaptive: records in same family with SDR similarity ≥ 0.15 → merged in first pass; singletons in second pass with relaxed threshold
    Test 6: TagFamilyBackoff: if all records in a family are singletons (no SDR merges), fall back to broad bucket key
    Test 7: TagFamilyDenseBackoff: detects stable secondary tags across records, creates dense corridor groups
    Test 8: denseCorridorStableTags: returns tags that appear in >50% of records
    Test 9: denseBackoffGroupKey: builds `${family}:${dense_corridor}` key from stable secondary tags
  </behavior>
  <action>
    Per D-11, D-12: Implement BridgeKey normalize and TagFamily backoff strategies.

    **normalizeBridgeTag function:**
    - Static default mapping table (mirrors Rust hardcoded map):
      - "ui/frontend" → "frontend", "ui/backend" → "backend", "ui/dashboard" → "dashboard"
      - "auth/authentication" → "authentication", "auth/authorization" → "authorization", "auth/login" → "login"
      - "deploy/release" → "release", "deploy/deployment" → "deployment", "deploy/rollback" → "rollback"
      - "db/database" → "database", "db/postgresql" → "database", "db/redis" → "cache", "db/mongodb" → "database"
      - "api/rest" → "api", "api/graphql" → "api", "api/grpc" → "api"
      - "test/unit" → "test", "test/integration" → "test", "test/e2e" → "test"
      - "monitor/alert" → "alert", "monitor/metric" → "metric", "monitor/log" → "log"
      - "ci/build" → "build", "ci/test" → "ci", "ci/deploy" → "deploy"
    - Accept optional override map (passed at construction or via setter)
    - normalizeBridgeTag(tag: string, overrides?: Record<string, string>): string
    - If overrides has the tag → use override
    - If default table has the tag → use default
    - Otherwise → return tag unchanged

    **TagFamilyAdaptive:** (2-pass)
    - Pass 1: apply standard TagFamily key (alphabetically first tag)
    - Run sdr_subcluster with threshold 0.15
    - For any record that ended up as a singleton (its own cluster) AND has additional tags:
    - Pass 2: try relaxed threshold (0.10) with TagFamily key using those additional tags
    - If merge succeeds → reassign to the merged group

    **TagFamilyBackoff:**
    - Start with TagFamily key (alphabetically first tag)
    - Run sdr_subcluster with threshold 0.15
    - If ALL clusters are singletons (every record is its own cluster):
    - Fall back to "broad bucket" — re-key with a more general family (first tag prefix before "/" or "_")
    - Re-run subcluster with the broad key

    **TagFamilyDenseBackoff:**
    - Start with TagFamily key
    - For records in the same family: detect stable secondary tags via denseCorridorStableTags
    - denseCorridorStableTags(records): compute frequency of each tag beyond the family tag; return tags present in >50% of records
    - denseBackoffGroupKey(family, stableSecondaryTags): `${family}:${stableSecondaryTags.join(",")}`
    - Re-group records with the dense corridor key
    - Run sdr_subcluster within each dense corridor group

    **claim_key_with_mode integration:**
    - TagFamilyAdaptive: dispatch to adaptive 2-pass in update_with_sdr
    - TagFamilyBackoff: dispatch to backoff logic in update_with_sdr
    - TagFamilyDenseBackoff: dispatch to dense backoff logic in update_with_sdr

    Write tests before implementation (TDD RED phase).
  </action>
  <acceptance_criteria>
    - normalizeBridgeTag("ui/frontend") returns "frontend"
    - normalizeBridgeTag("unknown/tag") returns "unknown/tag"
    - normalizeBridgeTag("ui/frontend", { "ui/frontend": "ui" }) returns "ui" (override wins)
    - TagFamilyBackoff: records with tags ["auth","config"] and ["auth","permission"] where each is singleton → fall back and merge under "auth" bucket
    - denseCorridorStableTags: 5 records with tags ["alpha","x","y"], ["alpha","x","z"], ["alpha","x","w"] → returns ["x"] (present in 3/3 > 50%)
    - "bun run test --filter '@aura/belief'" passes all P2 tests
  </acceptance_criteria>
  <verify>
    <automated>cd packages/belief && bun run typecheck 2>&1 && bun run test --reporter verbose 2>&1</automated>
  </verify>
  <done>BridgeKey normalize + 3 TagFamily backoff strategies implemented; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: apply_layer_feedback rewrite with bounded clamping + BeliefFeedbackReport</name>
  <files>
    packages/belief/src/BeliefEngine.ts
    packages/belief/src/BeliefEngine.test.ts
  </files>
  <read_first>
    @../src/belief.rs (lines 1287-1350 for apply_layer_feedback)
    @packages/belief/src/BeliefEngine.ts
    @packages/contract/src/belief/BeliefTypes.ts (for BeliefReport field names)
  </read_first>
  <behavior>
    Test 1: apply_layer_feedback accepts CausalEngine + PolicyEngine and returns BeliefFeedbackReport
    Test 2: apply_layer_feedback reads causal patterns and policy hints, computes confidence_delta
    Test 3: confidence_delta is clamped to [-0.18, +0.08]
    Test 4: volatility_delta is clamped to [-0.06, +0.20]
    Test 5: stability is NOT modified by apply_layer_feedback (reserved for resolve())
    Test 6: feedback applied to distinct beliefs from both causal and policy signals
    Test 7: Report shows beliefsTouched, beliefsBoosted, beliefsDampened, netConfidenceDelta, netVolatilityDelta
  </behavior>
  <action>
    Per D-03 and AUDIT-DIFF 1.7: Rewrite apply_layer_feedback to match Rust's algorithm.

    New signature:
    `(causalEngine: CausalEngineImpl, policyEngine: PolicyEngineImpl) => Effect<BeliefFeedbackReport, never, EpistemicTrace>`

    Algorithm (mirroring Rust belief.rs:1287-1350):
    1. Get causal engine state (stats()) → iterate patterns
       - For each stable pattern with causal_strength > 0.5: positive confidence_delta
       - For each pattern where cause_belief_id matches a known belief_id
    2. Get policy engine state (stats()) → iterate hints
       - For each stable hint with policy strength > 0.55: positive confidence_delta if Prefer/Recommend
       - For Avoid/VerifyFirst hints: negative confidence_delta
       - For Suppressed hints: no contribution
    3. Aggregate per belief_id:
       - Sum confidence_deltas from all signals
       - Sum volatility_deltas from contradictory signals (conflicting polarity on same belief)
    4. Apply:
       - Clamp confidence_delta: max(-0.18, min(0.08, delta))
       - Clamp volatility_delta: max(-0.06, min(0.20, delta))
       - Update belief.confidence += confidence_delta
       - Update belief.volatility += volatility_delta
    5. Do NOT modify stability (stability is ONLY changed by resolve())
    6. Build BeliefFeedbackReport with each entry

    BeliefFeedbackReport type (add to file or reuse from existing contract types):
    - beliefsTouched: number
    - beliefsBoosted: number
    - beliefsDampened: number
    - netConfidenceDelta: number
    - netVolatilityDelta: number
    - entries: ReadonlyArray<FeedbackAuditEntry>

    Each FeedbackAuditEntry:
    - beliefId, sourceKind, sourceId, reason
    - deltaRequested (before clamp), deltaApplied (after clamp)
    - confidenceBefore, confidenceAfter
    - volatilityBefore, volatilityAfter
    - stabilityBefore, stabilityAfter (both unchanged — for completeness)

    Update BeliefEngineImpl:
    - Remove the old apply_layer_feedback that accepted {belief_id, action, factor}[]
    - Implement the new signature

    Write tests before implementation (TDD RED phase).
  </action>
  <acceptance_criteria>
    - apply_layer_feedback returns BeliefFeedbackReport with all fields populated
    - confidence delta clamped: requesting +0.20 → actual +0.08
    - confidence delta clamped: requesting -0.25 → actual -0.18
    - stability NOT modified after feedback: belief.stability same before and after
    - beliefsTouched > 0 when causal engine has stable patterns
    - Report shows per-belief entries with sourceKind, deltaApplied, confidenceBefore/After
    - "bun run test --filter '@aura/belief'" passes all tests
  </acceptance_criteria>
  <verify>
    <automated>cd packages/belief && bun run typecheck 2>&1 && bun run test --reporter verbose 2>&1</automated>
  </verify>
  <done>apply_layer_feedback rewritten with bounded clamping and BeliefFeedbackReport; BeliefEngine complete; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Layer feedback | Belief confidence modified by causal/policy engine signals |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-06 | Tampering | Bounded clamping | mitigate | Delta clamped to Rust-defined ranges; stability excluded from modification |
| T-06.3-07 | Spoofing | BridgeKey normalize | accept | Override map supported; default table defines deterministic mappings |
| T-06.3-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
- `bun run test --filter "@aura/belief"` passes all P0+P1+P2 tests
- All 8 BeliefEngine deviations from AUDIT-DIFF closed
</verification>

<success_criteria>
- BridgeKey normalize with default mapping table (30+ bridge pairs)
- TagFamilyAdaptive/Backoff/DenseBackoff all implemented
- apply_layer_feedback accepts CausalEngine + PolicyEngine, returns BeliefFeedbackReport
- Bounded clamping: [-0.18, +0.08] for confidence, [-0.06, +0.20] for volatility
- All P0+P1+P2 tests pass
- AUDIT-DIFF.md BeliefEngine section all entries updated to fixed
</success_criteria>

<output>
`.planning/phases/06.3-engine-algorithm-parity/06.3-04-SUMMARY.md` when done
</output>
