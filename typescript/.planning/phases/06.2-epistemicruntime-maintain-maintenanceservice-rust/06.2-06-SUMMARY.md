---
plan: 06.2-06
phase: 06.2
status: complete
tasks_completed: 4
tasks_total: 4
---

## Plan 06.2-06: MaintenanceService — 14 Effect Functions

### What was built

Implemented `MaintenanceService` in `@aura/core` with 14 Effect functions covering the full maintenance cycle orchestration. Replaces the inline `maintain()` that was removed from EpistemicRuntime. Aligned with Rust `maintenance_service.rs` per D-05, D-06, D-07.

### Tasks

1. **Create MaintenanceService.ts** (936 lines) — 14 Effect functions + 5 constants + internal helpers:
   - 5 exported constants: `MAINTENANCE_TREND_LIMIT` (32), `REFLECTION_SUMMARY_LIMIT` (16), plus internal limits
   - `runInitialPhases` — orchestrates levelFix → decay → reflect → epistemicState → insights via Effect.gen
   - `buildSdrLookup` — builds SDR lookup cache with Ref-based caching
   - `computeLayerStability` — compares key_index diffs for 4 layers (retained/new/dropped/churn)
   - `runDiscoveryPhases` — belief → concept||causal (Effect.all concurrency:2) → policy → feedback
   - `runPostDiscoveryPhases` — consolidation → crossConnections → taskReminders → archival (stubs for algorithm-heavy)
   - `finalizeTelemetry` — finds dominant phase, reads concept surface telemetry
   - `buildTrendSnapshot` — pure function, 14-field snapshot
   - `pushTrendSnapshot` / `summarizeTrends` — bounded history (32 entries) + aggregation
   - `buildReflectionSummary` — 3 finding types (blocker/contradiction/trend)
   - `pushReflectionSummary` / `summarizeReflections` — bounded history (16 entries) + aggregation
   - 4 report conversion helpers for internal → external type mapping

2. **Implement core orchestration** — Combined with Task 1. Concept and causal discovery parallel via `Effect.all({ concurrency: 2 })` (D-13). Stub phases marked per D-07.

3. **Create MaintenanceService.test.ts** (511 lines) — 16 tests: 5 for computeLayerStability (retained/new/dropped/churn, empty state), 8 for pure functions (buildTrendSnapshot, summarizeTrends), 3 for bounded history enforcement.

4. **Update @aura/core index.ts** — Added `export * from "./MaintenanceService"`

### Key files

| File | Status | Lines |
|------|--------|-------|
| packages/core/src/MaintenanceService.ts | NEW | 936 |
| packages/core/src/MaintenanceService.test.ts | NEW | 511 (16 tests) |
| packages/core/src/index.ts | MODIFY | Added MaintenanceService export |

### Deviations

- Post-discovery phases (consolidation, crossConnections, taskReminders, archival) are stubs — algorithm logic deferred per D-07 (requires TagTaxonomy, NGramIndex not yet in TS)
- buildReflectionSummary has stub implementation for blocker/contradiction/trend finding generation
- Some internal type imports use placeholder aliases for module types not yet exported

### Verification

- 16 tests passing covering computeLayerStability, buildTrendSnapshot, summarizeTrends, bounded history
- typecheck passes with zero errors in packages/core/
- All 14 function signatures match PROJECTION.md section 2 specifications
- Constants match Rust values exactly
- Effect.all concurrency: 2 for concept||causal parallelism
