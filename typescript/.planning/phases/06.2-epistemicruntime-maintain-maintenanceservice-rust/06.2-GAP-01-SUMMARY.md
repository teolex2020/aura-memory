---
phase: 06.2
plan: GAP-01
subsystem: policy-epistemic
tags: [surface-types, contract-imports, type-migration, enum-alignment]

requires:
  - phase: 06.2
    plan: 01
    provides: "PolicyActionKind and PolicyState enums in @aura/contract/policy/PolicyTypes.ts"
  - phase: 06.2
    plan: 01
    provides: "SurfacedPolicyHint type in @aura/contract/EpistemicInspection.ts"
provides:
  - "Surface.ts uses canonical contract types for SurfacedPolicyHint, PolicyActionKind, PolicyState"
  - "Surface.test.ts compiles and passes all 12 tests with enum-based type references"
  - "EpistemicRuntime.ts no longer imports PolicyState transitively from @aura/policy"
affects: [core, epistemic-runtime, mcp]

tech-stack:
  added: []
  patterns: ["Surface functions import contract types directly instead of defining local copies"]

key-files:
  modified:
    - packages/policy/src/Surface.ts
    - packages/policy/src/Surface.test.ts
    - packages/epistemic-runtime/src/EpistemicRuntime.ts

key-decisions:
  - "Removed unused SurfacePolicyState import from EpistemicRuntime.ts entirely rather than redirecting to @aura/contract"

requirements-completed: [REQ-011]

duration: 12min
completed: 2026-05-28
---

# Phase 06.2 Plan GAP-01: Contract Type Migration for Policy Surface Functions

**Surface.ts now imports canonical contract types (SurfacedPolicyHint, PolicyActionKind, PolicyState) from @aura/contract instead of defining local copies, closing the verification gap identified in VERIFICATION.md**

## Performance

- **Duration:** 12 min
- **Started:** 2026-05-28T08:19:25Z
- **Completed:** 2026-05-28T08:30:45Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Replaced 3 local type definitions (PolicyActionKind, PolicyState, SurfacedPolicyHint) in Surface.ts with canonical imports from @aura/contract
- Updated ACTION_KIND_PRIORITY to use computed property names with enum values (PolicyActionKind.Avoid, etc.)
- Removed actionKindString() helper (no longer needed since enum values are already lowercase)
- Removed extra fields (confidence, triggerConceptIds, triggerBeliefIds, supportingRecordIds) from toSurfaced() to match the narrower contract SurfacedPolicyHint
- Added re-exports of PolicyActionKind, PolicyState, and SurfacedPolicyHint for downstream consumers of @aura/policy
- Updated Surface.test.ts to use enum member references (PolicyActionKind.Prefer, PolicyState.Stable, etc.) and removed assertions for removed fields
- Removed unused SurfacePolicyState import from EpistemicRuntime.ts

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace local types with contract imports in Surface.ts** - `ae1b837` (feat)
2. **Task 2: Update Surface.test.ts to match contract types** - `7611bec` (test)
3. **Task 3: Update EpistemicRuntime.ts imports for removed exports** - `2ada39a` (fix)

## Files Modified
- `packages/policy/src/Surface.ts` - Replaced local type definitions with contract imports; removed actionKindString helper; simplified toSurfaced mapping; added re-exports
- `packages/policy/src/Surface.test.ts` - Updated imports and callers to use enum references; removed assertions for removed fields (triggerConceptIds, triggerBeliefIds, supportingRecordIds)
- `packages/epistemic-runtime/src/EpistemicRuntime.ts` - Removed unused `import type { PolicyState as SurfacePolicyState } from "@aura/policy"`

## Decisions Made
- Removed unused SurfacePolicyState import from EpistemicRuntime.ts entirely instead of redirecting to `@aura/contract`, since the type was not referenced anywhere in the file
- PolicyHint and PolicyEngine local interfaces retained (they are internal engine types, not contract types)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- **Worktree path mismatch:** Initial edits were accidentally applied to the wrong directory (`D:/workspace/@yuyi919/external/AuraSDK/typescript/packages/` instead of the worktree path). Corrected by re-applying edits to the worktree version at `D:/workspace/@yuyi919/external/AuraSDK/.claude/worktrees/agent-aca383fa4a0efa007/typescript/packages/`.
- **Edit tool whitespace sensitivity:** The Edit tool fails when old_string whitespace doesn't exactly match the file. Resolved by using sed for bulk replacements with consistent whitespace.

## Pre-existing Type Errors (Deferred)

The following type errors exist in files not modified by this plan and are deferred:

| File | Error | Notes |
|------|-------|-------|
| packages/policy/src/PolicyEngine.ts | Missing properties from contract PolicyHint (actionKind, policyStrength, etc.) | PolicyEngine creates hints without required fields; predates this plan |
| packages/storage/src/PolicyStoreFile.ts | Missing key_index property from PolicyEngineState | PolicyEngineState was extended with key_index in phase 06.2-01 |
| packages/storage/src/PolicyStoreFile.test.ts | Same as above | |
| packages/core/src/MaintenanceService.test.ts | Ref import / Effect type issues | Pre-existing compatibility issue |

These errors are documented for awareness. They do not affect the modified Surface.ts, Surface.test.ts, or EpistemicRuntime.ts files.

## Next Phase Readiness

- Surface functions now use canonical contract types, closing the VERIFICATION.md gap
- PolicyActionKind enum alignment ensures consistent runtime values across the codebase
- Ready for GAP-02 (wire Aura.runMaintenance to MaintenanceService) if applicable

---

*Phase: 06.2-epistemicruntime-maintain-maintenanceservice-rust*
*Plan: GAP-01*
*Completed: 2026-05-28*
