---
phase: 07-mcp-polish
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - packages/core/src/MaintenanceService.ts
  - packages/core/src/Aura.ts
  - packages/core/src/MaintenanceService.test.ts
  - packages/storage/src/*
autonomous: false
requirements: [REQ-001, REQ-012]
must_haves:
  truths:
    - All remaining MaintenanceService unknown placeholders are replaced with real typed dependencies or verified local shims
    - D-07 maintenance stubs needed for maintain/insights/memory_health/explainability parity are implemented or reduced to accurately bounded residual gaps
    - Trend/reflection outputs are persisted through the new storage helpers, not held only in ephemeral arrays
    - Constants, thresholds, and formulas are verified against Rust source during execution rather than copied from planning prose
---

<objective>
Finish the maintenance-service debt that was folded into Phase 7: replace zombie dependency placeholders, implement the remaining D-07 maintenance algorithms required for operator-facing MCP tools, and persist maintenance trend/reflection outputs so later governance/explainability APIs have real inputs.
</objective>

<context>
@.planning/phases/07-mcp-polish/07-RESEARCH.md
@packages/core/src/MaintenanceService.ts
@packages/core/src/MaintenanceService.test.ts
@packages/storage/src/PersistenceManifest.ts
@../src/maintenance_service.rs
@../src/aura.rs
</context>

<tasks>
- Replace the `unknown` placeholders in `MaintenanceService.ts` with real imports, real service contracts, or narrowly scoped typed adapters proven against Rust source.
- Implement the remaining stubbed sections in `runInitialPhases`, `buildSdrLookup`, feedback handling, `runPostDiscoveryPhases`, `finalizeTelemetry`, and `buildReflectionSummary` to the degree required for Phase 7 MCP parity.
- Persist `MaintenanceTrendSnapshot[]` and `ReflectionSummary[]` using the new storage helpers, and wire `Aura.runMaintenance()` so one cycle updates those artifacts deterministically.
- Remove or consolidate stale D-07 markers so the remaining comments document only real, still-open gaps.
- Add targeted tests covering maintenance history persistence, reflection/trend generation, and any newly real algorithms introduced in this plan.
</tasks>

<verification>
- `bun run typecheck`
- `bun run test --filter "@aura/core"`
- `rg "type .* = unknown|TODO: Full algorithm deferred per D-07" packages/core/src/MaintenanceService.ts`
</verification>

<success_criteria>
- `MaintenanceService.ts` no longer relies on `unknown` placeholders.
- Maintenance outputs required by governance/health/explainability tools are persisted.
- The Phase 7 executor can trust `maintain` to produce usable downstream data rather than just a skeleton report.
</success_criteria>

<output>
Create `.planning/phases/07-mcp-polish/07-02-SUMMARY.md` when done.
</output>
