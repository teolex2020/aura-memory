# Phase 06.3: Engine Algorithm Parity - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-28
**Phase:** 06.3-engine-algorithm-parity
**Areas discussed:** 实现优先级和顺序, 对齐深度, Hypothesis策略, 实现策略, Belief内部顺序, 验证策略, Rust源码获取, 测试策略, Causal信号源, Causal评分, Policy输入, CanonicalFeature实现, Surface对齐, Policy Suppression, Recommendation文本, BridgeKey配置, TagFamily回退

---

## 实现优先级和顺序

| Option | Description | Selected |
|--------|-------------|----------|
| 按优先级逐批修复 | P0 → P1 → P2 顺序，中间状态可能打破现有测试 | |
| 逐引擎完整对齐 | Belief全部 → Concept全部 → Causal全部 → Policy全部，独立验证 | ✓ |
| 先打通验证链 (P0 only) | 只修 P0，P1/P2 作为后续 Phase | |
| 四引擎并行推进 | 每个引擎都有人改，每日 fixture 验证 | |

**User's choice:** 逐引擎完整对齐
**Notes:** 每个引擎独立验证稳定后再进入下一个，避免"改了一半又去改另一个"的上下文切换

---

## 对齐深度

| Option | Description | Selected |
|--------|-------------|----------|
| 逐行 1:1 翻译 | TS代码结构与Rust逐函数对应，常量值、公式、分支条件完全一致 | ✓ |
| 语义等价 | 相同输入→相同输出，内部实现可以用TS习惯写法 | |
| 关键路径对齐 (P0+P1) | 只修 P0+P1，P2 留到后续版本 | |

**User's choice:** 逐行 1:1 翻译
**Notes:** 可维护性最高，Rust diff → TS diff 可追踪

---

## Hypothesis 生成策略

| Option | Description | Selected |
|--------|-------------|----------|
| 改为 Rust contradiction 分裂 | supporting/opposing 两组，最多2个hypothesis，确定性ID | ✓ |
| 保留 SDR 聚类 + 修阈值 | 保持现有Union-Find架构，阈值降到0.15，ID改确定性哈希 | |

**User's choice:** 改为 Rust contradiction 分裂
**Notes:** 架构级变更，替换现有 Union-Find SDR 聚类

---

## 代码实现策略

| Option | Description | Selected |
|--------|-------------|----------|
| 从 Rust 源码重写 | 逐函数翻译所有算法逻辑 | |
| 以 AUDIT-DIFF 为 checklist 修复 | 逐项修复差异点，改动量最小 | |
| 混合策略 | P0重写 + P1/P2 checklist | ✓ |

**User's choice:** 混合策略
**Notes:** P0 架构级变更从源码重写，P1/P2 参数/守卫/公式按 checklist 修复

---

## Belief 内部修复顺序

| Option | Description | Selected |
|--------|-------------|----------|
| Belief内部也按P0→P1→P2 | 架构先稳定，再补守卫，最后P2 | |
| 按Rust源码函数顺序 | 从 belief.rs 第一个函数开始逐行翻译 | ✓ |
| 只修 P0+P1 (P2暂缓) | P2 用 SIMPLE 标记 | |

**User's choice:** 按Rust源码函数顺序
**Notes:** 保持与 Rust 源码相同的阅读顺序，利于后续维护

---

## 验证策略

| Option | Description | Selected |
|--------|-------------|----------|
| 每引擎验证后推进 | 修完一个验一个 | |
| 全部修完后统一验证 | 四个引擎全修完再统一跑 Rust fixture | ✓ |
| 逐项验证 | 每修完一个 AUDIT-DIFF 项就加对应测试 | |

**User's choice:** 全部修完后统一验证
**Notes:** 端到端验证完整性更好

---

## Rust 源码获取方式

| Option | Description | Selected |
|--------|-------------|----------|
| 直接读取Rust源码 | 频繁参考 Rust 源码逐函数翻译 | ✓ |
| 以EXPLAIN.md为准 | 不直接读 Rust 源码 | |
| AUDIT-DIFF + EXPLAIN 结合 | 两者互补 | |

**User's choice:** 直接读取Rust源码
**Notes:** 逐行 1:1 翻译需要 Rust 源码作为翻译蓝本

---

## 测试策略

| Option | Description | Selected |
|--------|-------------|----------|
| Fixture parity 测试为主 | Rust fixture 驱动 + 保留改写已有测试 | ✓ |
| 保留现有测试 + 新增fixture测试 | 不改动现有测试 | |
| 不新增测试 | 只用已有 bun run test | |

**User's choice:** Fixture parity 测试为主
**Notes:** 保留并改写已有 TS 单元测试，新增边界条件测试

---

## CausalEngine 信号源

| Option | Description | Selected |
|--------|-------------|----------|
| 全部三种边来源 | explicit caused_by_id + causal connection + temporal edges | ✓ |
| 只实现 explicit edges | 时序边后续补 | |
| 全部三种 + 保留概念共现 | concept 共现作为 secondary signal | |

**User's choice:** 全部三种边来源
**Notes:** NearbySuccessors budgeting (每cause最多16个successors) + 每namespace最多5000条边

---

## CausalPattern 评分体系

| Option | Description | Selected |
|--------|-------------|----------|
| 完整 20+ 字段 | transition_lift, temporal_consistency, outcome_stability, causal_strength 等 | ✓ |
| 精简 10 字段 | 只新增 transition_lift 和 causal_strength | |

**User's choice:** 完整 20+ 字段
**Notes:** 包含 evidence gates (support/repeated/counterfactual) + corpus_fingerprint

---

## PolicyEngine 输入扩展

| Option | Description | Selected |
|--------|-------------|----------|
| 三引擎输入 | causal_engine + concept_engine + belief_engine + records | ✓ |
| 保持现有输入 + 新增polarity | 只加 polarity 分类不改输入 | |

**User's choice:** 三引擎输入
**Notes:** 完整实现 Rust 的 build_belief_to_concepts + aggregate_belief_confidence + belief_gate

---

## ConceptEngine CanonicalFeature

| Option | Description | Selected |
|--------|-------------|----------|
| 完整实现 | stemming(15种后缀) + equivalence dict(80+映射) + Jaccard + cluster guards | ✓ |
| Stemming + guards (词典后续补) | 等价词典逐步积累 | |
| 骨架实现 (SIMPLE标记) | 仅骨架，后续补全 | |

**User's choice:** 完整实现
**Notes:** Jaccard 替代 SDR Tanimoto 作为默认模式，停用词从 60+ 扩展到 80+

---

## ConceptEngine Surface 对齐

| Option | Description | Selected |
|--------|-------------|----------|
| 完整对齐 | per-namespace cap + dedup + 5维tiebreak + candidate阈值 | ✓ |
| 只修cap和阈值 | 其他 tiebreak 细节后续处理 | |

**User's choice:** 完整对齐
**Notes:** MAX_SURFACED_PER_NAMESPACE=5, SURFACE_CANDIDATE_THRESHOLD=0.70

---

## PolicyEngine Suppression

| Option | Description | Selected |
|--------|-------------|----------|
| 完整实现 | 冲突检测 + 抑制 policy_strength 低者 | ✓ |
| 只检测不抑制 | 只标记冲突对，不修改 hint state | |

**User's choice:** 完整实现
**Notes:** 同 namespace+domain 冲突检测 + 正负极性冲突 + cause_record_ids 重叠 → 抑制

---

## Recommendation 文本

| Option | Description | Selected |
|--------|-------------|----------|
| 对齐 Rust 模板 | 5种模板（Avoid/VerifyFirst/Prefer/Recommend/Warn） | ✓ |
| 结构化格式 | 简短结构化 recommendation | |

**User's choice:** 对齐 Rust 模板
**Notes:** 模板变量使用 cause_key 和 domain

---

## BridgeKey normalize 桥接表

| Option | Description | Selected |
|--------|-------------|----------|
| 完整映射桥接表 | 硬编码 Rust 的桥接映射 | |
| 可配置桥接表 | 从配置读取，默认提供 Rust 相同映射 | ✓ |

**User's choice:** 可配置桥接表
**Notes:** 默认映射：ui/frontend→frontend, auth/authentication→authentication, deploy/release→release 等

---

## TagFamily 回退策略

| Option | Description | Selected |
|--------|-------------|----------|
| 三个都实现 | TagFamilyAdaptive + TagFamilyBackoff + TagFamilyDenseBackoff | ✓ |
| 只实现 Backoff | 其他后续补 | |
| Adaptive + Backoff | DenseBackoff 后续补 | |

**User's choice:** 三个都实现
**Notes:** 完整对齐 Rust 的 12 种 CoarseKeyMode

---

## Claude's Discretion

- Rust 源码中具体常量的提取和映射（桥接表、关键词表、停用词表、等价词典表的具体条目）
- 每个引擎内部函数的 TS 翻译细节（在签名和算法已锁定的前提下）
- 测试用例的边界条件设计
- Phase 内 plan 的拆分粒度和 wave 编排

## Deferred Ideas

- MaintenanceConfig 的 runtime 动态调整（热更新配置）
- EpistemicRuntime 的 ConceptSurfaceMode 运行时切换
- 引擎性能优化（并行化、缓存、增量计算）
