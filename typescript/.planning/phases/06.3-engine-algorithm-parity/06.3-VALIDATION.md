---
phase: 06.3
slug: engine-algorithm-parity
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-28
---

# Phase 06.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.0+ (with @effect/vitest) |
| **Config file** | `vitest.config.ts` per package |
| **Quick run command** | `cd packages/{engine} && bun run test` |
| **Full suite command** | `bun run test` (workspace root) |
| **Estimated runtime** | ~30 seconds per engine, ~120 seconds full suite |

---

## Sampling Rate

- **After every task commit:** Run `bun run test -- --run` for the affected engine package(s)
- **After every plan wave:** Run full `bun run typecheck && bun run test`
- **Before `/gsd-verify-work`:** Full suite + Rust fixture parity verification must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| *TBD by planner* | — | — | REQ-011, REQ-012 | N/A | N/A | unit+fixture | `bun run test` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Verify `bun run typecheck` passes on all `@aura/*` packages before starting
- [ ] Verify `bun run test` passes on all `@aura/*` engine packages before starting
- [ ] Rust fixture tools (`aura-ts-fixtures`, `aura-ts-verify-recall`) confirmed operational
- [ ] All 4 engine test files identified and current test counts recorded

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Rust fixture cross-language parity | REQ-011, REQ-012 | Requires Rust toolchain + fixture generation | Run Rust fixture generator on known brain directory, run TS maintenance cycle, compare reports field-by-field |
| Deterministic ID verification | REQ-011 | Requires cross-run comparison | Run same input twice, verify Belief/Concept/Causal/Policy IDs are identical across runs |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
