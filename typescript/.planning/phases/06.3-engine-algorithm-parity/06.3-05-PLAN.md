---
phase: 06.3-engine-algorithm-parity
plan: 05
type: tdd
wave: 3
depends_on: [06.3-04]
files_modified:
  - packages/concept/src/ConceptEngine.ts
  - packages/concept/src/ConceptEngine.test.ts
  - packages/concept/src/index.ts
autonomous: true
requirements: [REQ-011, REQ-012]
user_setup: []

must_haves:
  truths:
    - "ConceptSimilarityMode.CanonicalFeature is the default mode (matching Rust)"
    - "CanonicalFeature mode uses stemming (suffix rules) + equivalence dictionary (extracted from Rust concept.rs) + Jaccard similarity"
    - "Stemming removes common suffixes: suffix rules and canonicalization table extracted from Rust source"
    - "Equivalence dictionary maps domain synonyms — ALL entries extracted from Rust concept.rs equivalence map"
    - "Jaccard similarity computed over canonical token sets (stemmed + equivalence-resolved tokens)"
    - "SdrTanimoto mode preserved as fallback"
    - "Term extraction uses split_whitespace + trim_matches(!is_alphanumeric)"
    - "Stop word list expanded — entries extracted from Rust source (not hand-curated)"
  artifacts:
    - path: packages/concept/src/ConceptEngine.ts
      provides: "CanonicalFeature stemming, equivalence dictionary, Jaccard similarity, expanded stop words"
      contains: "stemWord, applyEquivalenceDictionary, jaccardSimilarity, normalizeTokens"
    - path: packages/concept/src/ConceptEngine.test.ts
      provides: "Tests for CanonicalFeature mode, stemming, equivalence dict, Jaccard"
  key_links:
    - from: packages/concept/src/ConceptEngine.ts
      to: @../src/concept.rs
      via: "Rust concept.rs CanonicalFeature implementation — ALL word lists, maps, and rules extracted directly from source"
---

<objective>
Implement ConceptEngine P1: CanonicalFeature mode with stemming (suffix rules), equivalence dictionary (extracted from Rust), Jaccard similarity, expanded stop words (extracted from Rust), and updated term extraction.

Purpose: Close AUDIT-DIFF 2.1, 2.3 — the CanonicalFeature implementation gap and term extraction differences.
Output: ConceptEngine.ts with CanonicalFeature mode, matching Rust concept.rs behavior.
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06.3-engine-algorithm-parity/06.3-CONTEXT.md
@.planning/phases/06.3-engine-algorithm-parity/06.3-RESEARCH.md
@AUDIT-DIFF.md

# Rust reference — ALL tables/word lists MUST be extracted directly from Rust source
@../src/concept.rs

# Current concept engine
@packages/concept/src/ConceptEngine.ts
@packages/concept/src/ConceptEngine.test.ts
@packages/concept/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stemming (suffix rules) + equivalence dictionary + Jaccard similarity + default CanonicalFeature mode</name>
  <files>
    packages/concept/src/ConceptEngine.ts
    packages/concept/src/ConceptEngine.test.ts
  </files>
  <read_first>
    @../src/concept.rs (search for stemming rules, suffix removal, equivalence map, canonicalization table, stop words list)
    packages/concept/src/ConceptEngine.ts
  </read_first>
  <behavior>
    Test 1: stemWord removes "ing" suffix per Rust rule
    Test 2: stemWord removes "ed" suffix per Rust rule
    Test 3: stemWord removes "ly" suffix per Rust rule
    Test 4: stemWord handles irregular forms via canonicalization table extracted from Rust
    Test 5: applyEquivalenceDictionary maps terms per Rust equivalence dictionary
    Test 6: applyEquivalenceDictionary returns unknown word unchanged
    Test 7: jaccardSimilarity(["a","b","c"], ["a","b","d"]) returns 2/4 = 0.5
    Test 8: jaccardSimilarity(["a"], ["a"]) returns 1.0
    Test 9: jaccardSimilarity([], ["a"]) returns 0.0
    Test 10: CanonicalFeature mode is default (not SdrTanimoto)
    Test 11: buildCanonicalTokens: applies stemming + equivalence + stop word filter
    Test 12: Stop word list matches Rust set (extracted from concept.rs)
  </behavior>
  <action>
    Per D-13, D-16: Implement CanonicalFeature mode as a new alternative similarity path.

    CRITICAL — ALL entries for the equivalence dictionary, stop word list, stemming suffix rules, and canonicalization table MUST be extracted directly from `@../src/concept.rs`. Do NOT hand-curate or transcribe from AUDIT-DIFF.md, EXPLAIN.md, or prose summaries below. The lists below are GUIDES — verify each entry against Rust source:

    1. Read concept.rs and search for:
       - Stop word list (look for `STOP_WORDS`, `stop_words`, or inline set)
       - Equivalence/hash map (look for `EQUIV`, `synonyms`, `canonical_map`)
       - Stemming rules (look for suffix stripping, `ends_with`, `trim_end_matches`)
       - Canonicalization table (look for irregular word mappings)
    2. Copy each entry exactly as it appears in Rust (same input→output pairs)
    3. If Rust has entries NOT listed in the guide below, include them too
    4. If the guide has entries NOT in Rust, do NOT add them

    **Stemming (suffix rules — extract from Rust source):**
    - stemWord(word: string): string
    - Suffix rules applied in order (first match wins):
      Guide (verify each against Rust):
      1. "ing" → remove if word.length > 4 (running→runn, but check canonicalization table first)
      2. "ed" → remove if word.length > 3 (deployed→deploy)
      3. "ly" → remove if word.length > 3 (quickly→quick)
      4. "es" → remove if word.length > 3 (watches→watch)
      5. "er" → remove if word.length > 3 (driver→driv)
      6. "s" → remove if word.length > 3 (cats→cat, NOT for "ss"/"us")
    - Canonicalization table (extract EXACT entries from Rust — guide values are approximate):
      - "running" → "run", "swimming" → "swim"
      - "deployed" → "deploy", "deploying" → "deploy"
      - "databases" → "database", "queries" → "query"
      - "analyses" → "analysis", "synopses" → "synopsis"
      - "better" → "good", "worse" → "bad"

    **Equivalence Dictionary (ALL entries extracted from Rust source):**
    - Static Record<string, string> mapping domain synonyms to canonical forms
    - Read concept.rs equivalence map from source — extract EVERY entry
    - Do NOT add entries that only exist in this guide but not in Rust
    - Guide categories (verify each entry against Rust):
      - Deploy: "deployment" → "deploy", "deploying" → "deploy", "deployed" → "deploy"
      - Database: "db" → "database", "postgresql" → "database", "mysql" → "database", "mongodb" → "database", "sqlite" → "database", "rdbms" → "database"
      - Cache: "redis" → "cache", "memcached" → "cache", "caching" → "cache"
      - Auth: "authentication" → "auth", "authorization" → "auth", "login" → "auth", "signin" → "auth"
      - API: "rest" → "api", "restful" → "api", "graphql" → "api", "grpc" → "api", "endpoint" → "api"
      - UI: "frontend" → "ui", "front-end" → "ui", "user-interface" → "ui"
      - Test: "testing" → "test", "unit-test" → "test", "integration" → "test", "e2e" → "test"
      - Build: "building" → "build", "compilation" → "build", "compile" → "build"
      - Monitor: "monitoring" → "monitor", "alerting" → "alert", "logging" → "log"
      - Network: "networking" → "network", "http" → "protocol", "https" → "protocol", "tcp" → "protocol"
      - Error: "error" → "error", "exception" → "error", "failure" → "error", "bug" → "defect"
      - Config: "configuration" → "config", "setting" → "config", "parameter" → "config"
    - Apply dictionary AFTER stemming (stem first, then map equivalence)

    **Jaccard Similarity:**
    - jaccardSimilarity(tokensA: string[], tokensB: string[]): number
    - |A ∩ B| / |A ∪ B|
    - If both empty → 1.0 (matching Rust)
    - If one empty → 0.0

    **buildCanonicalTokens(content: string): string[]**
    - Split content by whitespace: content.split(/\s+/).filter(Boolean)
    - For each word: toLowerCase → trim non-alphanumeric chars on both ends → stemWord → applyEquivalenceDictionary
    - Filter out stop words (extracted from Rust — verify count and entries)
    - Filter out tokens shorter than 2 chars
    - Return unique sorted canonical tokens

    **Stop word list (extract from Rust concept.rs):**
    - Read Rust source for the exact stop word set
    - Rust uses a HashSet<String> with entries; extract every unique entry
    - If Rust has >80 stop words, include all of them
    - If Rust has <80 stop words, do NOT add extras beyond what Rust has
    - Guide entries (verify each against Rust): 
      - Base existing ~60 stop words (keep existing ones if they match Rust)
      - Additional entries found in Rust but not in current TS: extract and add

    **ConceptEngine.discover integration:**
    - In discover(), check similarity_mode:
      - If CanonicalFeature: use buildCanonicalTokens + jaccardSimilarity for centroid construction and clustering
      - If SdrTanimoto: keep existing behavior (SDR-based)
    - DEFAULT to CanonicalFeature (matching Rust)

    Write tests before implementation (TDD RED phase). Include at least one test that compares the TS stop word set against Rust's stop word set to detect drift.
  </action>
  <acceptance_criteria>
    - stemWord("running") matches Rust output (canonicalized, not mechanical suffix removal)
    - applyEquivalenceDictionary entries match Rust source (grep-verified), not prose transcriptions
    - jaccardSimilarity(["a","b"], ["a","c"]) returns 1/3 ≈ 0.333
    - buildCanonicalTokens pipeline matches Rust token normalization
    - ConceptEngine defaults to CanonicalFeature mode
    - Stop word list matches Rust source set exactly
    - "bun run test --filter '@aura/concept'" passes all CanonicalFeature tests
  </acceptance_criteria>
  <verify>
    <automated>cd packages/concept && bun run typecheck 2>&1 && bun run test --reporter verbose 2>&1</automated>
  </verify>
  <done>Stemming, equivalence dictionary (extracted from Rust), Jaccard similarity, expanded stop words (extracted from Rust) all implemented; CanonicalFeature mode is default; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Canonical token extraction | Content processed for concept formation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-08 | Tampering | Stop word filter | mitigate | Stop word list extracted from Rust source; no code injection vector |
| T-06.3-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
- `bun run test --filter "@aura/concept"` passes all tests
- All equivalence dict entries, stop words, and stemming rules grep-verified against Rust source
- CanonicalFeature mode produces deterministic results for same input
</verification>

<success_criteria>
- Stemming with suffix rules + canonicalization table extracted from Rust
- Equivalence dictionary extracted from Rust (all entries, no additions, no omissions)
- Jaccard similarity function
- buildCanonicalTokens content pipeline
- CanonicalFeature is default similarity mode
- Stop word list extracted from Rust source
- All tests pass
</success_criteria>

<output>
`.planning/phases/06.3-engine-algorithm-parity/06.3-05-SUMMARY.md` when done
</output>
