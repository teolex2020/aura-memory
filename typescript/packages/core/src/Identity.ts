/**
 * The tag used for persona records in brain storage.
 *
 * @zh brain storage 中 agent persona record 使用的 tag。
 *
 * Rust reference: `PERSONA_TAG` (`../src/identity.rs`).
 */
export const PERSONA_TAG = "agent-persona"

/**
 * The tag used for user profile records in brain storage.
 *
 * @zh brain storage 中 user profile record 使用的 tag。
 *
 * Rust reference: `PROFILE_TAG` (`../src/identity.rs`).
 */
export const PROFILE_TAG = "user-profile"
