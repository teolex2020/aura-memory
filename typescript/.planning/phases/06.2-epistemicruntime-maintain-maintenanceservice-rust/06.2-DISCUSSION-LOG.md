# Phase 06.2: MaintenanceService + EpistemicRuntime 重构 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-28
**Phase:** 06.2-epistemicruntime-maintain-maintenanceservice-rust
**Areas discussed:** MaintenanceService 范围, EpistemicRuntime 深度, 语义投影策略, 包与契约, 投影深度, 契约变更, DI 模式, 投影产出, Surface Telemetry, 包重命名, Surface 辅助函数, 投影输出形式, 实现优先级, 测试策略, Config 方式, 并行策略

---

## MaintenanceService 范围

| Option | Description | Selected |
|--------|-------------|----------|
| 完整对齐 Rust 4 阶段 + 运维周边 | runInitialPhases → Discovery → PostDiscovery + telemetry/trend/reflection | |
| 核心发现链 + feedback | belief→concept//causal→policy→feedback | |
| 最小迁移 | 只移动现有 maintain() | |
| 先做完整语义投影，再决定 | 先映射 Rust 全部语义到 TS 类型/接口，再讨论实现范围 | ✓ |

**User's choice:** 先做完整语义投影，再决定
**Notes:** 用户要求"完整"对齐，先投影全部类型再决策实现范围

---

## EpistemicRuntime 深度

| Option | Description | Selected |
|--------|-------------|----------|
| 全量 1:1 对齐 Rust | 20+ inspection 方法：BeliefInstabilitySummary, ContradictionCluster, PolicyLifecycleSummary, PolicyPressureReport, Surface 方法 (带调用计数) | ✓ |
| 最小修复 | 只补齐现有 2 个 stub | |
| 核心 inspection + 健康摘要 | 核心方法 + instability_summary，跳过高级分析 | |

**User's choice:** 全量 1:1 对齐 Rust

---

## 语义投影策略

| Option | Description | Selected |
|--------|-------------|----------|
| 完整类型投影 → 分批实现 | Step 1: 逐方法映射 Rust → TS type，Step 2: 按优先级分批实现 | |
| 语义等价 — TS 惯用设计 | 用 Effect-TS 惯用方式重新设计等价 API | |
| 逐行翻译 Rust → TS | 直接参照 Rust 代码逐行翻译，保持方法签名和计算逻辑完全一致 | ✓ |

**User's choice:** 逐行翻译 Rust → TS

---

## 包与契约

| Option | Description | Selected |
|--------|-------------|----------|
| 新 @aura/maintenance 包 | 新建独立包 | |
| 同包拆分 — @aura/epistemic-runtime | 与 EpistemicRuntime 同包 | |
| 放在 @aura/core | 与 Aura facade 放在一起（Rust 中 MaintenanceService 是内部实现） | ✓ |

**User's choice:** 放在 @aura/core

---

## 投影深度

| Option | Description | Selected |
|--------|-------------|----------|
| 全量映射 — 类型 + 配置 + 指标 | MaintenanceConfig, PhaseTimings, MaintenanceHotspots, LayerStability, TrendSnapshot/Summary, ReflectionSummary/Digest 等 | (用户直接确认) |

**User's choice:** 完整对齐（类型+配置+指标），暂时不实现算法，先出文档再计划

---

## 契约变更

| Option | Description | Selected |
|--------|-------------|----------|
| 直接移除 maintain() | 从 EpistemicRuntime.Interface 中移除，所有下游引用同步更新 | ✓ |
| 保留 deprecated stub | 渐进迁移 | |
| 保留 maintain() — 内部委托 | 委托给 MaintenanceService，对外 API 不变 | |

**User's choice:** 直接移除 maintain()

---

## DI 模式

| Option | Description | Selected |
|--------|-------------|----------|
| Effect 函数 — 无 Tag，用 gen | 对齐 Rust 中通过参数传入依赖的模式 | ✓ |
| Tag + Layer — Context DI | 标准 Effect-TS 服务模式 | |
| 普通 class — 构造函数注入 | 非 Effect DI | |

**User's choice:** Effect 函数 — 无 Tag，用 gen

---

## 投影产出

| Option | Description | Selected |
|--------|-------------|----------|
| 类型映射文档 (PROJECTION.md) | Rust struct → TS type 对照表 | (后续选择) |
| 在 CONTEXT.md 中列清单 | 单文件 | ✓ (初步选择) |
| 清单级别 — 文件+方法索引 | 轻量级 | |

**User's choice (后续确认):** PROJECTION.md 类型映射 + CONTEXT.md 决策

---

## Surface Telemetry

| Option | Description | Selected |
|--------|-------------|----------|
| 用 Effect Ref 建模 — 保留计数 | 对齐 Rust AtomicU64 语义 | ✓ |
| 丢弃 telemetry 计数器 | 用日志替代 | |
| 暂不决策 | 留给实现阶段 | |

**User's choice:** 用 Effect Ref 建模 — 保留计数

---

## 包重命名

| Option | Description | Selected |
|--------|-------------|----------|
| 保持包名 | @aura/epistemic-runtime 不变 | ✓ |
| 重命名 | @aura/epistemic-inspection 等 | |
| 拆分为两个包 | inspection + maintenance | |

**User's choice:** 保持包名

---

## Surface 辅助函数

| Option | Description | Selected |
|--------|-------------|----------|
| 放在对应引擎包 | @aura/concept 导出 surfaceConcepts，@aura/policy 导出 surfacePolicyHints | ✓ |
| EpistemicRuntime 内部实现 | 私有函数 | |

**User's choice:** 放在对应引擎包 — 对齐 Rust 模块

---

## 投影输出形式

| Option | Description | Selected |
|--------|-------------|----------|
| PROJECTION.md 类型映射 + CONTEXT.md 决策 | 两份文档 | ✓ |
| 全部入 CONTEXT.md | 单文件 | |
| TS 类型草稿文件 + CONTEXT.md | 代码级 | |

**User's choice:** PROJECTION.md + CONTEXT.md

---

## 实现优先级

| Option | Description | Selected |
|--------|-------------|----------|
| 类型投影 → Inspection → Maintenance | 3 阶段 | ✓ |
| 类型 + Inspection 一起 → Maintenance | 2 阶段 | |
| 全部一起 | 单 Phase | |

**User's choice:** 类型投影 → Inspection → Maintenance

---

## 测试策略

| Option | Description | Selected |
|--------|-------------|----------|
| Rust fixture 驱动 — 跨语言一致性 | fixture 生成 → TS 读取 → 断言一致 | ✓ |
| 纯 TS 单元测试 | mock 引擎 | |
| 混合 | Inspection 用 Rust，编排用 TS mock | |

**User's choice:** Rust fixture 驱动

---

## Config 方式

| Option | Description | Selected |
|--------|-------------|----------|
| 可选参数 — 有默认值 | MaintenanceConfig 作为函数参数传入 | ✓ |
| Effect Context Tag | 可替换 | |
| 硬编码 | 全开，不做配置 | |

**User's choice:** 可选参数 — 有默认值

---

## 并行策略

| Option | Description | Selected |
|--------|-------------|----------|
| Effect.all 并行 — concept // causal | concurrency: 2 | ✓ |
| 串行 — 先保证正确性 | 线性链 | |

**User's choice:** Effect.all 并行

---

## Claude's Discretion

- 测试用例边界条件设计
- 方法实现细节（在签名和算法已锁定的前提下）
- Phase 内 plan 拆分粒度

## Deferred Ideas

- decay/reflect/consolidation/archival 算法实现 → 需要 TagTaxonomy、NGramIndex 等依赖
- MaintenanceConfig 运行时动态调整
- EpistemicRuntime 的 ConceptSurfaceMode 运行时切换
