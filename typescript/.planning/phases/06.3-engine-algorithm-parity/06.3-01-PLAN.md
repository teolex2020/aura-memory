---
phase: 06.3-engine-algorithm-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contract/src/causal/CausalTypes.ts
  - packages/contract/src/policy/PolicyTypes.ts
  - packages/contract/src/belief/BeliefTypes.ts
  - packages/contract/src/Causal.ts
  - packages/contract/src/Policy.ts
  - packages/contract/src/index.ts
autonomous: true
requirements: [REQ-011, REQ-012]
user_setup: []

must_haves:
  truths:
    - "CausalPattern type has 20+ scoring fields matching Rust"
    - "PolicyHint type has all Rust fields (polarity, actionKind enum, recommendation text)"
    - "CausalEngine.discover signature accepts BeliefEngine.Interface instead of ConceptEngineState"
    - "PolicyEngine.discover signature accepts 3 engines (CausalEngine.Interface + ConceptEngine.Interface + BeliefEngine.Interface) + records"
    - "CausalEngineState has new statistics fields matching Rust"
    - "BeliefEngineState has key_index and record_index fields"
    - "BeliefReport has 7+ fields matching Rust BeliefReport"
    - "All new types exported from @aura/contract index"
    - "TemporalBudgetMode, EvidenceMode, PolicyActionKind use TS string enums (not ad hoc string unions)"
    - "CausalPattern has cause_record_ids and effect_record_ids provenance fields"
    - "Workspace-level bun run typecheck passes (contract + all downstream packages)"
  artifacts:
    - path: packages/contract/src/causal/CausalTypes.ts
      provides: "Expanded CausalPattern (20+ fields), CausalEngineState, CausalReport"
      contains: "transition_lift, temporal_consistency, outcome_stability, causal_strength, cause_record_ids, effect_record_ids"
    - path: packages/contract/src/policy/PolicyTypes.ts
      provides: "PolicyHint with polarity, recommendation, utilityScore, riskScore, cause_record_ids, effect_record_ids fields"
      contains: "classify_polarity return type, PolicyActionKind string enum"
    - path: packages/contract/src/belief/BeliefTypes.ts
      provides: "BeliefEngineState with key_index, record_index; expanded BeliefReport"
      contains: "key_index: Readonly<Record<string, string>>"
    - path: packages/contract/src/Causal.ts
      provides: "Updated CausalEngine.discover signature"
      contains: "BeliefEngine.Interface"
    - path: packages/contract/src/Policy.ts
      provides: "Updated PolicyEngine.discover signature"
      contains: "discover: (causal_engine: CausalEngine.Interface, concept_engine: ConceptEngine.Interface, belief_engine: BeliefEngine.Interface, records)"
  key_links:
    - from: packages/contract/src/Causal.ts
      to: packages/contract/src/Belief.ts
      via: "import type { BeliefEngine }"
      pattern: "import.*BeliefEngine.*from.*Belief"
    - from: packages/contract/src/Policy.ts
      to: packages/contract/src/Causal.ts
      via: "import type { CausalEngine }"
      pattern: "import.*CausalEngine"
    - from: packages/contract/src/Policy.ts
      to: packages/contract/src/Concept.ts
      via: "import type { ConceptEngine }"
      pattern: "import.*ConceptEngine"
---

<objective>
Expand contract types to match Rust's rich type definitions — CausalPattern (20+ fields), PolicyHint (polarity/action/recommendation), BeliefEngineState (key_index/record_index), and update engine discover signatures per D-03 (1:1 Rust translation).

Purpose: Enable downstream engine implementations to reference the full Rust-aligned types without type errors.
Output: Updated @aura/contract types with all Rust fields mapped.
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/AuraSDK/typescript/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06.3-engine-algorithm-parity/06.3-CONTEXT.md
@.planning/phases/06.3-engine-algorithm-parity/06.3-RESEARCH.md
@AUDIT-DIFF.md

# Contract files to modify
@packages/contract/src/causal/CausalTypes.ts
@packages/contract/src/policy/PolicyTypes.ts
@packages/contract/src/belief/BeliefTypes.ts
@packages/contract/src/Causal.ts
@packages/contract/src/Policy.ts
@packages/contract/src/index.ts

# Project convention: ALL enum-like Rust equivalents MUST use TS string enums, never ad hoc string unions
# PROJECT RULE: Interfaces in @aura/contract MUST reference *.Interface types, never concrete *Impl types
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CausalTypes.ts — CausalPattern (20+ fields) + CausalEngineState + CausalReport</name>
  <files>
    packages/contract/src/causal/CausalTypes.ts
    packages/contract/src/Causal.ts
    packages/contract/src/index.ts
  </files>
  <read_first>
    packages/contract/src/causal/CausalTypes.ts
    packages/contract/src/Causal.ts
    packages/contract/src/index.ts
  </read_first>
  <action>
    Per D-03 and D-19: Expand CausalPattern to include all Rust scoring fields. Update Causal.ts discover signature using CausalEngine.Interface (NOT CausalEngineImpl). Add new types: CausalEdge (for record-level edges), TemporalBudgetMode string enum, EvidenceMode string enum. Add provenance fields cause_record_ids and effect_record_ids to CausalPattern for PolicyEngine suppression dependency.

    **Causal.ts discover signature change:**
    CausalEngine.Tag (or discover method) from:
      `(concept_state: ConceptEngineState, records: ReadonlyMap<string, AuraRecord>, sdr_lookup: SdrLookup)`
    to:
      `(belief_engine: BeliefEngine.Interface, records: ReadonlyMap<string, AuraRecord>, sdr_lookup: SdrLookup)`
    Add import for BeliefEngine type. Remove import for ConceptEngineState (no longer needed).

    CausalPattern additions (per D-19, matching Rust causal.rs):
    - cause_belief_id: string (new — replaces antecedent_concept_ids for belief-level)
    - effect_belief_id: string (new — replaces consequent_concept_ids for belief-level)
    - cause_key: string (new — namespace:cause_belief_key:effect_belief_key:edge_hash)
    - effect_key: string (new)
    - edge_hash: string (new)
    - transition_lift: number (new — P(effect|cause)/P(effect), min-max normalized to [0,1])
    - temporal_consistency: number (new — positive_gaps / total_pairs)
    - outcome_stability: number (new — 1 - variance/mean)
    - causal_strength: number (new — weighted combination)
    - support_count: number (new — raw edge count)
    - explicit_support_count: number (new — explicit edges only)
    - counterevidence_count: number (new)
    - temporal_windows: number (new — unique temporal windows)
    - namespace: string (new)
    - cause_record_ids: ReadonlyArray<string> (NEW — provenance for Policy suppression conflict detection per D-24; needed by Plans 09-10)
    - effect_record_ids: ReadonlyArray<string> (NEW — provenance for Policy polarity classification per D-22; needed by Plans 09-10)
    - Kept existing fields: id, support, confidence, lift, state, last_updated

    CausalEdge type (new):
    - cause_record_id: string
    - effect_record_id: string
    - namespace: string
    - edge_kind: "explicit" | "explicit_causal" | "temporal"
    - gap_seconds: number (for temporal edges)
    - created_at: number

    Add TemporalBudgetMode as TS string enum (PROJECT RULE: must use string enum, not ad hoc union):
    - ExhaustiveCapped = "ExhaustiveCapped"
    - NearbySuccessors = "NearbySuccessors"

    Add EvidenceMode as TS string enum (PROJECT RULE):
    - StrictRepeatedWindows = "StrictRepeatedWindows"
    - TemporalClusterRecovery = "TemporalClusterRecovery"
    - ExplicitTrusted = "ExplicitTrusted"

    CausalEngineState additions:
    - patterns: Record<string, CausalPattern> (already exists, field types expanded)
    - Add edges_found_total: number
    - Add temporal_budget_mode: TemporalBudgetMode
    - Add evidence_mode: EvidenceMode
    - Add last_corpus_fingerprint: string
    - Keep version: 1, discovery_mode

    CausalReport additions:
    - Add edge extraction stats: explicit_edges, temporal_edges, temporal_namespaces_scanned, temporal_pairs_considered, temporal_pairs_skipped_by_budget, temporal_edges_capped, temporal_namespaces_hit_cap
    - Add evidence gate stats: patterns_meeting_support_gate, patterns_meeting_repeated_window_gate, patterns_meeting_counterfactual_gate, patterns_blocked_by_evidence_gates, patterns_blocked_by_counterfactual_gate, avg_causal_strength, stable_count, rejected_count
    - Keep: patterns_found, patterns_active, patterns_invalidated, avg_confidence, avg_lift

    Export all new types from packages/contract/src/index.ts.
  </action>
  <acceptance_criteria>
    - CausalPattern has all 20+ fields: transition_lift, temporal_consistency, outcome_stability, causal_strength, support_count, explicit_support_count, counterevidence_count, temporal_windows, namespace, cause_belief_id, effect_belief_id, cause_key, effect_key, edge_hash, cause_record_ids, effect_record_ids PLUS existing fields
    - Provenance fields cause_record_ids and effect_record_ids are ReadonlyArray<string> (available for Policy suppression in Plans 09-10)
    - CausalEdge type exists with fields: cause_record_id, effect_record_id, namespace, edge_kind, gap_seconds, created_at
    - TemporalBudgetMode is a TS string enum (not "ExhaustiveCapped" | "NearbySuccessors" union)
    - EvidenceMode is a TS string enum (not "StrictRepeatedWindows" | "TemporalClusterRecovery" | "ExplicitTrusted" union)
    - CausalEngineState has edges_found_total, temporal_budget_mode, evidence_mode, last_corpus_fingerprint fields
    - CausalReport has edge extraction and evidence gate stats fields
    - bun run typecheck passes workspace-wide (packages/contract + packages/belief + packages/causal + packages/policy + packages/core)
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck 2>&1 | head -50</automated>
  </verify>
  <done>CausalTypes.ts has all new types with correct fields; Causal.ts uses BeliefEngine.Interface; index.ts exports them all; workspace-wide typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Expand PolicyTypes.ts + Policy.ts + BeliefTypes.ts</name>
  <files>
    packages/contract/src/policy/PolicyTypes.ts
    packages/contract/src/Policy.ts
    packages/contract/src/belief/BeliefTypes.ts
    packages/contract/src/index.ts
  </files>
  <read_first>
    packages/contract/src/policy/PolicyTypes.ts
    packages/contract/src/Policy.ts
    packages/contract/src/belief/BeliefTypes.ts
    packages/contract/src/index.ts
  </read_first>
  <action>
    Per D-21 and D-22: Expand PolicyTypes.ts, Policy.ts, and BeliefTypes.ts. Use Interface types throughout — CausalEngine.Interface and ConceptEngine.Interface, NOT CausalEngineImpl/ConceptEngineImpl.

    **PolicyTypes.ts additions:**
    - PolicyHint additions: polarity: "Positive" | "Negative" | "Neutral", recommendation: string, utilityScore: number, riskScore: number, cause_key: string, domain: string, effect_keys: ReadonlyArray<string>
    - PolicyHint already has actionKind, policyStrength, namespace, domain — these are correct
    - PolicyActionKind: Convert to TS string enum (PROJECT RULE) with values: "avoid", "verify_first", "prefer", "recommend", "warn". Export as:
      ```typescript
      export enum PolicyActionKind {
        Avoid = "avoid",
        VerifyFirst = "verify_first",
        Prefer = "prefer",
        Recommend = "recommend",
        Warn = "warn"
      }
      ```
    - PolicyReport additions: seeds_found, stable_hints, suppressed_hints, rejected_hints, avg_policy_strength

    **Policy.ts changes:**
    - Change PolicyEngine discover Tag signature from:
        `(causal_state: CausalEngineState, records: ReadonlyMap<string, AuraRecord>)` 
      to:
        `(causal_engine: CausalEngine.Interface, concept_engine: ConceptEngine.Interface, belief_engine: BeliefEngine.Interface, records: ReadonlyMap<string, AuraRecord>)`
      NOTE: Use CausalEngine.Interface and ConceptEngine.Interface — NOT CausalEngineImpl or ConceptEngineImpl (per project effect-smol layering rule: contracts define Interface Tags, implementations live in engine packages)
    - Add imports for CausalEngine, ConceptEngine, BeliefEngine types
    - Update PolicyStoreImpl if needed (no changes expected — store is opaque)

    **BeliefTypes.ts additions:**
    - BeliefEngineState additions: key_index: Readonly<Record<string, string>>, record_index: Readonly<Record<string, string>>
    - BeliefReport expansion: coarse_groups (already exists), beliefs_built (already exists), hypotheses_built (already exists). Add: beliefs_created: number, beliefs_pruned: number, revisions: number, resolved: number, unresolved: number, total_beliefs: number, total_hypotheses: number, churn_rate: number
    - Belief type: keep existing fields (id, key, hypothesis_ids, winner_id, state, score, confidence, support_mass, conflict_mass, stability, volatility, last_updated) — no changes needed
    - Hypothesis type: keep existing fields — no changes needed

    Export all new types from packages/contract/src/index.ts.
  </action>
  <acceptance_criteria>
    - PolicyHint has polarity, recommendation, utilityScore, riskScore, cause_key, domain, effect_keys fields
    - PolicyActionKind is a TS string enum with 5 values: Avoid/VerifyFirst/Prefer/Recommend/Warn
    - PolicyEngine discover Tag signature uses CausalEngine.Interface + ConceptEngine.Interface + BeliefEngine.Interface (NOT *Impl types)
    - BeliefEngineState has key_index and record_index fields
    - BeliefReport has 7+ fields matching Rust
    - bun run typecheck passes workspace-wide (contract + belief + causal + policy + core)
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck 2>&1 | head -50</automated>
  </verify>
  <done>PolicyTypes.ts, Policy.ts, BeliefTypes.ts all updated with Rust-aligned types; Policy.ts uses *.Interface not *Impl; typecheck passes workspace-wide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| contract → engine | Type definitions used by all 4 engine packages |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-01 | Tampering | Type exports | mitigate | All new types validated via workspace-wide bun run typecheck across all 5 packages (contract, belief, causal, policy, core) |
| T-06.3-SC | Tampering | npm installs | mitigate | No new packages — all existing |
</threat_model>

<verification>
- `bun run typecheck` passes workspace-wide (contract + belief + causal + policy + core)
- All new types visible in packages/contract/src/index.ts exports
</verification>

<success_criteria>
- CausalTypes.ts has expanded CausalPattern (20+ fields including cause_record_ids/effect_record_ids provenance), CausalEdge, TemporalBudgetMode enum, EvidenceMode enum, updated CausalEngineState, CausalReport
- PolicyTypes.ts has expanded PolicyHint with polarity/recommendation/utilityScore/riskScore, PolicyActionKind string enum, updated PolicyReport
- Policy.ts has 3-engine discover signature using CausalEngine.Interface + ConceptEngine.Interface + BeliefEngine.Interface (NOT *Impl)
- BeliefTypes.ts has key_index/record_index on BeliefEngineState, expanded BeliefReport
- All types exported from index.ts
- Workspace-wide typecheck passes
</success_criteria>

<output>
`.planning/phases/06.3-engine-algorithm-parity/06.3-01-SUMMARY.md` when done
</output>
