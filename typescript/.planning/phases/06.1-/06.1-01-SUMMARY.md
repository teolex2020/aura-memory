---
plan: 06.1-01
phase: 06.1
type: fix
wave: 0
autonomous: true
requirements:
  - REQ-012
status: complete
completed_tasks: 1
issues: []
deviations: []
---

## Phase 06.1: Type Error Fixes — Complete

**Objective:** Fixed all TypeScript strict type errors across epistemic engine packages.

### What Changed

**CausalEngine.ts:**
- Lines 104-107: Replaced destructuring with explicit index access + non-null assertions for `conceptIds[i]` and `conceptIds[j]`

**BoundedReranker.ts:**
- Line 27: Added intermediate variable with non-null assertion before destructuring `reranked[i]`

**BoundedReranker.test.ts:**
- Lines 12, 24: Added non-null assertions for `result[0]` access

**EpistemicRuntime.ts (contract):**
- Widened `maintain()` R type to include `BeliefEngine | ConceptEngine | CausalEngine | PolicyEngine`
- Widened `get_beliefs/get_concepts/get_causal_patterns/get_policy_hints` R types to include respective engine services

**EpistemicRuntime.ts (implementation):**
- Aligned return type annotations with widened contract

**EpistemicRuntime.test.ts:**
- Mock engine functions now properly typed with `BeliefEngineImpl/ConceptEngineImpl/CausalEngineImpl/PolicyEngineImpl` return types
- Mock stats results cast to proper `ConceptEngineState/CausalEngineState` types

### Verification
- `bun run typecheck`: 0 errors in our packages
- `bun run test packages/causal/ policy/ epistemic-runtime/ recall/`: 41/41 tests pass
